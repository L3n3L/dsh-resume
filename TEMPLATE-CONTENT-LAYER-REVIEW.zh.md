# 模板正文层与视觉差异化对接复盘

## 1. 本轮目标

本轮不以“增加模板数量”作为目标，而是让现有 5 套模板真正服务简历内容：项目、实习、教育、技能和成果都要有可读的层级、节奏和视觉语义。

对齐对象是成熟简历模板的正文设计方法，不复制任何代码、命名、素材或版式。保留 dsh-resume 自己的模板 Schema、Layout IR、语义模块和 A4 测量闭环。

## 2. 现状复盘

当前模板已经具备独立 CSS 文件和稳定的正文钩子：

- `dsh-entry-title`：经历或项目条目标题；
- `dsh-entry-meta`：日期、岗位、技术栈等元信息；
- `dsh-entry-bullets`：条目要点；
- `dsh-skill-groups` / `dsh-skill-group`：分组技能；
- `dsh-resume-section`：模块边界。

此前的不足不是 CSS 文件不存在，而是正文元素覆盖不完整：表格、引用、代码、链接反馈、嵌套列表、条目分隔、打印场景和安全图标语义没有形成完整内容层。

## 3. 实施方案

### 3.1 公共正文能力

五套模板都补齐以下内容契约，并且全部使用 `[data-template-id="..."]` 作用域：

- 表格：表头底色、单元格边距、边框、斑马纹和横向溢出保护；
- 引用：强调色左条、低透明度背景和内外间距；
- 代码：等宽字体、圆角、浅色底和长文本换行；
- 链接：可读的默认样式、浏览器 hover 反馈和打印安全颜色；
- 列表：嵌套层级、要点间距和 marker 颜色；
- 技能分组：分组边框、标签和内容密度；
- 打印：页面背景、文字颜色、链接颜色和阴影的 PDF 安全策略。

这些规则是“内容基础设施”，但不强制五套模板长得一样。公共元素的具体颜色、圆角、边框密度和间距由模板自己的视觉系统决定。

### 3.2 五套模板的正文差异

| 模板 | 正文视觉方向 |
| --- | --- |
| 校招标准 | 左侧蓝条、浅蓝卡片、清晰编号，优先 ATS 可读性 |
| 肖像侧栏 | 玫瑰色个人品牌、柔和卡片、轻量分组 |
| 极客实验室 | 终端提示符、深色面板、等宽元信息 |
| 成果看板 | 项目成果卡片、证据分隔、结果优先 |
| 杂志开篇 | 编辑式标题、日期轨道、细边框和段落节奏 |

每套模板只增加服务内容的设计规则，不通过堆装饰制造差异。

### 3.3 安全图标语义

不引入 iconfont，也不允许外部 URL。Markdown 中可使用受控 token，例如 `[icon:github]`，渲染器只把白名单 token 转成：

```html
<span class="dsh-icon dsh-icon-github" aria-label="GitHub"></span>
```

图标由模板 CSS 使用安全的内联 SVG data URI 或纯 CSS 几何符号绘制。未知 token 保持普通文本，避免 AI 输出任意 HTML 或外部资源。

## 4. 不采用的做法

- 不把 CSS 字节数、选择器数量当成质量结论，只作为回归预警；
- 不复制其他项目的 CSS、SVG、图标字体、命名或模板结构；
- 不使用 `!important` 压过渲染器契约；
- 不用外部字体、图片或网络资源；
- 不把 hover 当成 PDF 能力，浏览器态和打印态分别验收；
- 不为了“满一页”破坏字号、可读性和 A4 分页边界。

## 5. 验收标准

1. `node --test` 全部通过；
2. 每套模板都能渲染同一份饱满示例简历；
3. 表格、引用、代码、链接、嵌套列表和技能分组在 DOM 和浏览器视觉上生效；
4. 五套模板的项目/经历条目具有明显但合理的视觉差异；
5. 深色模板打印时切换到可读的浅色纸面，浅色模板不出现阴影或背景污染；
6. `[icon:github]` 等白名单 token 可渲染，未知 token 不产生 HTML 或网络请求；
7. A4 页数、溢出、留白和 content hash 关联不回退；
8. npm pack 能包含新增文档和 CSS 资产。

## 6. 交付边界

本轮完成“正文层能力 + 模板差异化 + 安全图标 token + 测试与浏览器验收”。模板数量、AI 自动生成完整 Renderer、复杂品牌图标库和高级分页算法不在本轮扩张，避免重新回到“功能很多但视觉不稳”的问题。
