# AI 模板工坊对接方案

## 1. 方案结论

插件不应该让 DeepSeek 每次直接生成一份自由发挥的 HTML/CSS。稳定方案是：

> DeepSeek 生成受约束的视觉模板规格，插件使用固定渲染器生成页面，再用 A4 测量结果反向校正。

CodeCV 可以作为产品交互参考：模板库、模板卡片、模板分类、选择后进入编辑和预览。但模板代码、DOM 结构、CSS、图标、截图和文案全部独立实现。

## 2. 产品定位

“AI 简历视觉设计师 + A4 排版调优器”。

用户不需要理解 CSS，只需要表达：

- 想投什么岗位；
- 想要正式、技术、简洁或有设计感；
- 希望单栏还是双栏；
- 希望内容紧凑还是舒展；
- 是否必须控制在一页。

AI 负责提出视觉方案，用户负责确认，渲染器负责保证页面可用。

## 3. 与 CodeCV 的借鉴边界

可以学习的产品机制：

- 模板入口和模板卡片；
- 模板分类和风格筛选；
- 模板缩略预览；
- 选择模板后进入编辑器；
- 模板有独立字体、颜色、间距和内容结构；
- 模板使用热度或推荐排序的产品思路。

必须独立实现：

- 模板 JSON Schema；
- Markdown 到页面的渲染器；
- A4 分页和测量逻辑；
- CSS、DOM、图标、缩略图和交互样式；
- AI 生成模板的提示词和修复流程。

不能直接复制 CodeCV 的源码、模板文件、CSS 选择器、页面结构、图片、Logo、截图或具体模板外观。

## 4. 模板分层

模板分为三层，避免 AI 从空白开始乱做：

### 4.1 基础骨架

程序固定以下能力：

- A4 页面尺寸；
- 单栏/双栏容器；
- 个人信息区；
- 教育、技能、项目、实习等模块；
- 分页和打印规则；
- 模块不拆分策略；
- 溢出和留白测量。

### 4.2 视觉 Token

AI 只能调整受控参数：

- 字体族；
- 正文字号；
- 标题字号；
- 行高；
- 页边距；
- 模块间距；
- 主色和辅助色；
- 分割线样式；
- 圆角、阴影和背景；
- 密度等级。

### 4.3 模块顺序和显示策略

AI 可以根据岗位和用户选择调整：

- 模块顺序；
- 是否显示某个模块；
- 项目标题的布局；
- 联系方式的排列方式；
- 双栏区域中哪些信息进入侧栏。

AI 不得删除用户内容，不得编造经历，也不得通过极小字号强行塞入一页。

## 5. 模板 Schema

DeepSeek 只输出 JSON，不直接输出 CSS：

```json
{
  "schemaVersion": 1,
  "name": "技术极简",
  "description": "适合前端、后端和算法岗位的高密度单栏模板",
  "layout": {
    "mode": "single-column",
    "pageTarget": 1,
    "density": "compact",
    "moduleOrder": ["profile", "education", "skills", "projects", "experience"]
  },
  "typography": {
    "fontFamily": "system-sans",
    "fontSize": 13,
    "headingScale": 1.18,
    "lineHeight": 1.42
  },
  "spacing": {
    "pageMargin": 42,
    "sectionGap": 12,
    "paragraphGap": 5
  },
  "visual": {
    "accentColor": "#26334d",
    "textColor": "#1f2937",
    "mutedColor": "#667085",
    "divider": "solid",
    "cornerRadius": 0
  }
}
```

Schema 必须经过程序校验：枚举值、数值范围、颜色格式、模块白名单和页数目标都不能由模型突破。

## 6. AI 生成流程

```text
用户描述风格
  ↓
读取当前简历结构和已有模板
  ↓
DeepSeek 输出模板 JSON
  ↓
Schema 校验与范围修正
  ↓
固定渲染器生成预览
  ↓
测量页数、溢出、模块断裂和留白
  ↓
最多 3 轮自动修复
  ↓
展示前后对比，等待用户应用
```

自动修复必须遵守顺序：

1. 调整模块间距；
2. 调整段落间距；
3. 调整页边距；
4. 调整字号和行高；
5. 最后才建议用户精简内容。

如果单页留白超过 12%，不能显示“一页通过”，而应显示“一页但偏空”。

## 7. 模板提示词约束

系统提示词要明确要求：

- 只返回符合 Schema 的 JSON；
- 不返回 Markdown 代码块；
- 不生成 HTML、JavaScript 或任意 CSS；
- 不修改用户简历内容；
- 不新增未在模块白名单中的结构；
- 不使用低于 11px 的正文；
- 不使用低于 1.2 的行高；
- 不以溢出换取“看起来一页”；
- 页面不合格时返回修复后的 JSON 和原因。

DeepSeek 的稳定性来自“结构化输出 + 白名单 + 固定渲染 + 测量回路”，而不是依赖一段很长的审美提示词。

## 8. 插件代码对接

建议新增：

- `lib/template-schema.js`：Schema、默认值和范围校验；
- `lib/template-presets.js`：内置模板元数据和预设；
- `lib/template-renderer.js`：把 JSON Token 转成 CSS 变量和布局类；
- `lib/template-validator.js`：模板生成后的视觉规则校验；
- `client/template-workshop.js`：模板卡片和 AI 生成入口。

建议新增工具：

- `jobhunt_template_list`：列出内置模板和用户模板；
- `jobhunt_template_generate`：根据风格描述生成模板 JSON；
- `jobhunt_template_apply`：应用模板到当前预览；
- `jobhunt_template_validate`：检查模板是否适合 A4 投递。

模板应用只保存模板设置和版本，不覆盖 `resume.md`。用户的内容和视觉方案必须可以独立撤销。

## 9. UI 设计

新增“模板工坊”入口，分为三块：

### 模板库

- A4 缩略图；
- 模板名称和适用岗位；
- 单栏/双栏标签；
- 紧凑/标准/舒展标签；
- “预览”“应用”按钮。

### AI 制作

输入框示例：

> 给我一个适合前端校招的单栏模板，黑白灰，技术感，内容密度高但不要拥挤。

AI 输出模板后先进入预览，不自动覆盖当前版本。

### 视觉调优

- 页数和留白结果；
- 字号、行高、模块间距、页边距；
- 应用建议；
- 撤销；
- 恢复默认；
- 保存为新模板。

## 10. 开发计划

### P0：稳定模板基础

1. 定义 Schema 和范围校验；
2. 把当前 default.css 改为 Token 驱动；
3. 增加 4 个原创内置模板；
4. 预览页支持模板参数；
5. 模板切换不修改简历内容；
6. 复用现有页数、留白、溢出测量。

### P1：AI 模板生成

1. 增加模板生成工具；
2. 增加结构化输出和失败重试；
3. 自动渲染并校正最多 3 轮；
4. 展示模板生成前后对比；
5. 保存、撤销和恢复模板版本。

### P2：模板资产能力

1. 用户保存个人模板；
2. 从自然语言继续修改模板；
3. 从参考截图提取设计原则后生成原创方案；
4. 模板导入导出；
5. 模板评分和真实投递反馈。

## 11. 验收标准

- DeepSeek 生成结果始终能通过 Schema 校验；
- 无效模板不会直接进入正式预览；
- 模板切换不会覆盖用户简历正文；
- 模板能在 A4 预览和打印中保持一致；
- 单页通过必须满足无溢出且留白不超过 12%；
- 用户可以撤销模板应用和参数调整；
- AI 生成失败时能回退到最近一次可用模板；
- 模板生成不依赖 CodeCV 的源码、资源或具体视觉表达。

## 12. 推荐的第一批原创模板

1. `campus-standard`：校招标准，单栏，清晰稳重；
2. `tech-compact`：技术极简，单栏，高密度；
3. `quiet-editorial`：低饱和、舒展层级，适合运营和产品岗位；
4. `mono-terminal`：黑白高对比，适合开发、测试和工程岗位。

这四套模板先作为 AI 的安全基线。AI 先从基线模板修改 Token，而不是每次从零创作，稳定性会明显更高。双栏和侧栏结构暂缓到 P1，等模块测量和打印回归稳定后再开放。

## 13. 当前已接入

- 已实现模板 Schema、范围校验和三套原创内置预设；
- 已提供 `jobhunt_template_list` 和 `jobhunt_template_validate`；
- `/dsh-resume/api/templates` 可供工作台读取模板列表；
- 工作台已提供带 A4 缩略图的模板卡片，并用不同视觉变体区分模板；
- 预览 URL 支持 `template` 参数，模板切换不会修改简历正文；
- 模板 Token 会和现有字号、行高、模块间距、页边距调节叠加；
- A4 页数、溢出和留白测量仍是最终通过条件。
