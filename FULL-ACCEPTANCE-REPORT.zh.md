# dsh-resume 全链路验收报告

## 验收结论

当前版本没有发现 P0 阻塞问题，核心工作流可以提测：进入工作台、切换模板、编辑 Markdown、读取 A4 页数、模板对比、AI 结构化提问、HTML 下载和窄屏布局均可运行。

但 AI 助手与主对话之间仍有两个需要优先对接的体验风险，PDF 导出还缺少自动化可见回执；这三项建议进入下一轮 P1 修复。

## 测试环境

- 服务：`http://localhost:3100/dsh-resume/`
- 工作区：秋招 / `companies/frontend/preview.html`
- 内容：林知远饱满示例简历
- 桌面视口：`1440 × 1000`
- 响应式视口：`800 × 900`、`360 × 800`
- 自动化：25 / 25 通过，`git diff --check` 通过，`npm pack --dry-run` 通过

## 通过项

| 场景 | 结果 | 证据 |
| --- | --- | --- |
| 进入求职简历工作台 | 通过 | 能从 Harness 入口进入工作台，不需要重复刷新 |
| 旧版/新版布局预览 | 通过 | 单栏、split、grid 均正常出现，A4 可见 |
| 模板库切换 | 通过 | 侧栏信号、作品集网格均能应用并回到预览 |
| 模板对比 | 通过 | 最多 3 个候选，第 4 个不会加入 |
| Markdown 实时编辑 | 通过 | 临时增加模块后 A4 DOM 出现，恢复原文后消失 |
| Shift+Enter | 通过 | AI 输入框保留换行 |
| Enter 发送 | 通过 | 消息进入主对话，助手任务流开始同步 |
| 结构化 Question | 通过 | 选项可选中、提交按钮解锁，结果出现在任务流 |
| Think / Tool call / Read | 通过 | 按时间顺序出现在 AI 助手任务流 |
| HTML 下载 | 通过 | 页面提示“HTML 已下载” |
| 投递版本切换 | 通过 | `preview.html` 与 `companies/frontend/preview.html` 可切换并重新测量 |
| 页数一致性 | 通过 | 顶部显示 2 页，A4 可访问结构同样有 2 个页面区域 |
| 窄屏响应式 | 通过 | 800px、360px 均无横向滚动溢出 |

## P1：建议优先修复

### P1-01 AI Question 存在“双份控制面”

复现：在 AI 助手里发送请求，主对话产生结构化 Question 后，页面同时能看到主 Harness 的 Question 控件和插件助手任务流中的 Question 控件。两边都展示相同问题，但用户不容易判断应该操作哪一个。

影响：用户可能重复回答、回答了镜像卡但主任务仍显示“等待回答”，或者误以为插件提交没有生效。

建议：插件只镜像主 Question 的只读状态；需要回答时，在插件内显示唯一操作卡，并明确标注“回答将提交到主对话”。如果宿主 Question 已经可见，则插件侧只显示同步状态和跳转/定位入口。

### P1-02 “停止等待”与“停止主任务”语义不一致

复现：插件侧停止/放弃等待后，主对话任务仍可能继续运行；本次验收中主任务在插件侧取消后仍继续读文件并生成候选，最后需要主对话自己的取消入口结束。

影响：用户以为已经取消，但后台仍在消耗模型时间和上下文；长任务尤其容易造成“插件没反应”的误解。

建议：按钮文案明确拆成“停止插件等待”和“停止主任务”；前者保留当前设计但必须持续显示“主对话仍在运行”，后者仅在 Harness 协议允许时调用主任务取消，否则不要伪装成取消。

### P1-03 PDF 导出缺少成功/失败回执

复现：点击“导出 PDF”后代码通过 `window.open(previewSrc, '_blank')` 再调用 `window.print()`。浏览器自动验收中没有暴露稳定的新标签或完成事件，也没有可确认的“已打开打印窗口”状态。

影响：弹窗被拦截、打印窗口未打开或用户关闭打印后，工作台都难以判断发生了什么。

建议：点击后立即显示“正在打开打印预览”；监听弹窗创建失败并保留“直接打开预览页打印”的备用按钮；打印页加入清晰标题和页数，不要把结果完全交给不可观测的 `window.print()`。

### P1-04 模板动作接口依赖 active root，未显式传递工作区 root

代码检查：`/dsh-resume/api/templates/actions` 使用 `defaultRoot()`，模板版本接口也依赖默认 root；而编辑器保存接口会显式传 `editorSource.root`。

影响：当用户同时打开多个工作区、active preview 已切换或服务重连后，复制/保存/恢复模板可能写入错误工作区，属于高风险的数据错位问题。

建议：模板 actions 的 body 统一携带 `root`，服务端校验并使用 `defaultRoot(body.root)`；versions 查询也传入 root。补一个双工作区 API 回归测试。

## P2：已知边界

### P2-01 双栏模块级分页仍可能留下较多空白

侧栏信号在饱满简历上测到 3 页、首屏留白约 34%。这是当前“完整模块作为分页原子”的结果，内容没有被截断，但还不是最优的页数压缩策略。

### P2-02 网格需要足够的模块粒度才能体现并列卡片

当前示例只有一个项目模块，所以项目卡片占满一行是正常结果；如果未来需要多个项目并列，需要 `project-item` 级结构或把项目拆成多个 IR item。

### P2-03 成功提示可能残留

HTML 下载后切换/刷新，页面仍可能保留“HTML 已下载”提示。建议在新操作开始时清理上一条 editor message，避免用户误把旧提示当成当前结果。

## 最终分级

- P0：0 个
- P1：4 个，其中 AI Question/停止语义最影响使用感受，模板 root 需要在多工作区场景优先修复
- P2：3 个，不阻塞当前提测

## 建议下一步

1. 先与 Harness 对齐 Question/取消协议，消除双份控制面。
2. 给模板 API 补 root 参数和双工作区隔离测试。
3. 给 PDF 导出加可观测回执。
4. 再做列表项级分页，解决双栏留白和一页命中率。
