# dsh-resume 第二期对接复盘

> 状态：开始实施
> 日期：2026-08-22
> 目标：把第一期“能读写、能渲染、能导出”升级为“能针对岗位准备一份可信、可检查、可回溯的投递版简历”。

> 主线调整：本文件保留质量检查方向；视觉调优主线见 [VISUAL-TUNING-REVIEW.zh.md](./VISUAL-TUNING-REVIEW.zh.md)。

## 1. 第一期复盘

### 已经打通

- Agent 可以初始化、读取、写入和渲染 `jobhunt/` 工作区。
- 主简历与 `companies/<name>/resume.md` 投递版分离。
- 用户可以在 DeepSeek Harness 内预览并打印 PDF。
- 预览采用 A4 纸张，显示页数和溢出提示。
- UI 已从单一浮层升级为“预览 / 投递版本 / 排版检查”工作台。

### 暴露的问题

1. **状态不够真实**：排版检查页之前主要是规则说明，不是当前简历的实际检查结果。
2. **JD 没有成为一等对象**：公司、岗位、JD、简历版本之间的关系主要靠目录约定，界面没有明确呈现。
3. **修改缺少解释**：用户很难知道 Agent 改了什么、哪些内容来自已有素材、哪些地方仍然缺证据。
4. **页面检查与内容检查割裂**：一页状态、联系方式、项目要点质量没有汇总成一个投递前结论。

## 2. 产品定位

本插件定位为：

> **面向学生求职的投递版简历工作台。**

它不自动海投，也不替用户编造经历。它服务的是一条明确链路：

```text
真实素材 → 目标 JD → 投递版简历 → 内容检查 → A4 排版检查 → 用户确认导出
```

职责分工：

| 角色 | 负责 | 不负责 |
| --- | --- | --- |
| Agent | 读取素材、分析 JD、改写投递版、解释修改、记录缺口 | 编造经历、替用户确认投递、自动发送 |
| 插件 | 管理文件关系、质量检查、版本预览、页数反馈、导出入口 | 替代用户判断真实性 |
| 用户 | 补充事实、确认版本、确认导出 | 不需要手工完成全部排版 |

## 3. 第二期范围

### 本轮实施

1. 抽取共享的 `resumeQualityCheck` 检查逻辑。
2. 增加 `GET /dsh-resume/api/check`，让界面读取当前投递版的真实检查结果。
3. `jobhunt_check` 与界面使用同一套检查结果，避免 Agent 和 UI 结论不一致。
4. 排版检查页展示实际警告、通过项和下一步建议。
5. 右侧状态卡显示真实检查状态，而不是固定文案。

### 下一小步

- 引入 `companies/<company>/meta.json`，记录公司、岗位、JD 来源和投递状态。
- 增加主简历与投递版的差异摘要。
- 增加基于 JD 关键词的“匹配/未覆盖/待补证据”检查。
- 增加可撤销的版本快照。

## 4. 质量检查设计

第一版只做确定性检查，不假装理解事实：

- 姓名一级标题是否存在；
- 邮箱和联系电话是否存在；
- 是否有二级标题模块；
- 项目要点数量；
- 是否存在过长要点；
- 是否包含明显占位符；
- 目标是校园求职一页 A4，但最终页数以浏览器实际渲染为准。

输出结构：

```json
{
  "passed": true,
  "score": 86,
  "checks": [
    { "id": "contact.email", "status": "pass", "message": "已发现邮箱" },
    { "id": "content.placeholders", "status": "warn", "message": "仍有占位内容" }
  ],
  "warnings": [],
  "next": "补齐量化结果后重新渲染"
}
```

## 5. UI 对接

### 排版检查页

- 顶部显示当前文件和整体状态；
- 中间显示检查项列表；
- 每项有通过、提醒或问题状态；
- 点击预览回到 A4 画布；
- 右侧显示“适合投递 / 需要补充 / 需要调整”。

### 状态原则

- 不用绿色表示“内容一定真实”，只表示检查项通过；
- 不把静态字符长度当作真实页数；
- 检查失败时给出可以执行的下一步，不只显示错误。

## 6. 安全与合规

- 所有检查在本地完成，不上传简历内容。
- 不复制 CodeCV 的代码、模板、图标或资源；只借鉴 A4 预览和编辑器的产品思路。
- 用户内容仍受工作区路径限制，预览接口只读取 `jobhunt/` 下允许的文本文件。
- 任何“疑似经历”都标记为待用户确认，不自动补写为事实。

## 7. 验收标准

- `jobhunt_check` 与 UI 的检查结果一致。
- 切换不同投递版后，排版检查结果随文件更新。
- 没有邮箱、电话或姓名时能看到对应提醒。
- 有占位符或超长项目要点时能看到可执行建议。
- A4 预览仍能正常缩放、分页和打印。
- Node 语法检查通过，现有 Host 工具行为不回归。
