# AI 助手进度镜像对接复盘

## 结论

简历插件内的 AI 助手不应该再“猜测 AI 正在做什么”，也不应该只等主对话最后返回一段文本。它应该作为当前主对话的一个轻量视图，实时反映主 Session 已经产生的可展示事件：Think、Read、Edit、Grep、Shell、Tool call、Question 和最终回复。

这不是再造一套 Agent，也不是复制主对话的全部界面。主对话仍然是唯一执行入口和事实来源，插件只负责把与简历任务有关的执行进度、确认问题和结果带回工作台。

## 本次复盘发现的问题

### 1. 原链路只有最终回复

原实现通过 `session.prompt()` 把任务送入主对话，但插件主要等待一个新的 assistant 节点，并且在 `snapshot.running` 结束后才展示结果。因此用户在 AI 执行 Read、Tool call 或排版检查期间只能看到“正在等待主对话”，无法判断任务是否真的在工作。

### 2. 不能用前端假进度替代真实事件

“正在读取简历 → 正在调整模板 → 正在检查一页”如果不是主 Session 实际产生的事件，就不应该在插件里编造。假进度会让用户误以为工具已经执行，出现超时或失败时也无法定位。

### 3. 不能把内部思维全文暴露成聊天内容

Think 可以作为一个短标签和折叠摘要展示，但不把完整内部推理链复制到插件。工具名称、目标文件、调用状态、错误和用户需要处理的 Question 可以如实展示；长文本只截断为摘要。

## 对接设计

### 数据源

- 当前主 Session 的 snapshot。
- `snapshot.nodes`：执行节点、工具节点、assistant 节点和用户确认相关节点。
- `snapshot.running`：当前是否仍在执行。
- `snapshot.pending`：是否有待用户处理的确认问题。
- 节点的 `seq`：区分本次插件请求之前和之后产生的事件。

插件不读取主对话 DOM，也不维护第二份 Agent 状态。Session 订阅更新后重新归一化节点，保证主对话和插件看到的是同一份进度。

### 事件映射

| 主对话事件 | 插件显示 | 展示内容 |
| --- | --- | --- |
| reasoning / think | Think | 简短摘要，默认收起 |
| read / 文件读取 | Read | 文件名或读取目标、完成状态 |
| edit / write / patch | Edit | 修改目标、完成状态 |
| grep / search | Grep | 搜索目标或关键词 |
| shell / exec / command | Shell | 命令目标和成功/失败 |
| 其他工具调用 | Tool call | 工具名、目标、状态 |
| pending question | Question | 需要用户选择或填写的内容 |
| assistant | Assistant | 最终答复或阶段性说明 |

如果运行时没有提供更细的事件类型，插件显示通用的 `Tool call` 或 `Event`，不通过字符串猜测出一个不存在的工具动作。当前实现先兼容节点和 block 中常见的 `name`、`toolName`、`tool.name`、`function.name` 字段，后续可以根据运行时稳定 schema 再收紧映射。

### 请求生命周期

每次从插件发送任务时记录：

- `requestId`：防止旧请求的响应污染新请求；
- `sessionId`：防止切换主对话后串入旧进度；
- `baselineSeq`：只把本次请求之后的节点作为本次任务时间线；
- `transport`：标记是直接 Session 对接还是兼容事件桥接。

请求完成后保留时间线，让用户能回看“AI 做过什么”；切换 Markdown 编辑任务时清空旧时间线，避免把上一份简历的执行过程带到下一份任务。

## 当前实现范围

本轮已开始落地：

1. 在主对话摘要中生成最近执行节点的归一化 `timeline`。
2. 在插件 AI 助手中增加紧凑的“主对话进度”区域。
3. 按 Think、Read、Edit、Grep、Shell、Tool call、Question 等类型显示状态点。
4. 运行中的最后节点、等待确认、错误节点使用不同状态色。
5. 时间线只显示当前请求产生的节点；没有请求时显示当前 Session 最近事件。
6. Question 仍使用原有选择卡提交，时间线只做同步提示，不复制一套确认协议。

## 后续迭代

### P0

- 对真实 Session 节点做稳定字段适配，补齐工具失败和取消状态。
- 增加时间线测试数据，覆盖长任务、连续工具调用、切换 Session、Question 阻塞。
- 将 45 秒固定超时改为“无事件 idle 超时”，避免工具调用较慢时被误报失败。

### P1

- 点击时间线事件时，在编辑器中定位对应文件或 Markdown 片段。
- 对 Read、Edit、Tool call 提供折叠详情，默认保持侧栏紧凑。
- 让主对话的阶段性 assistant 文本以增量方式显示，而不是只显示最后一条。
- 对工具调用增加取消当前任务入口，取消动作仍然交给主 Session 执行。

## 验收标准

- 主对话执行简历任务时，插件不再只有“等待”两个字，至少能看到真实的 Read、Tool call 或其他节点。
- 工具仍在执行时，最后一个节点显示运行中；工具完成后显示完成；失败时显示失败。
- 主对话提出选择题时，插件同时出现 Question 时间线和可操作选择卡。
- 切换 Session 后，旧任务不会继续更新当前插件。
- 关闭并重新打开编辑器不会把上一份简历的时间线带入新任务。
- 主对话没有可用工具类型时，显示通用事件，不编造具体动作。
- 插件不展示完整内部思维链，只展示用户理解任务状态所需的短摘要。
