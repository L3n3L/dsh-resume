# AI 助手桥接复盘与对接方案

## 目标

用户在简历工作台里提出“压缩项目经历”“改得更专业”“调整到刚好一页”等请求时，插件应当复用当前 Harness 主对话，而不是创建一个没有上下文的新聊天框，也不要求用户离开预览页确认。

理想体验是：

```text
简历工作台内提问
  → 进入当前 Harness Session
  → 主对话继续使用原有上下文、工具和模型
  → 工作台镜像 AI 回答
  → 如需用户确认，在工作台内显示选择卡片
  → 用户回答继续回到同一个 Session
  → 内容只进入草稿，用户确认后保存
```

## 本轮复盘

### 原方案的问题

第一版只在浏览器里派发 `dsh-resume:assistant-request`，等待 Harness 另一侧主动监听。这个协议可以作为跨宿主兜底，但它没有解决三个关键问题：

1. 插件不知道当前主对话是哪一个 Session。
2. 请求没有真正进入主对话的消息流，因此无法自然复用主对话上下文。
3. AI 需要用户选择时，只能把问题当作普通文本，无法在插件内完成回答。

如果宿主没有额外 listener，这条链路最终只能退化为“复制任务到主对话”。这不符合工作台的产品目标。

### 新方案

优先使用 Harness Client Runtime 已提供的正式能力：

- `ctx.sessions.currentProvideInfo`：读取当前主会话。
- 当前 session 的 `getSnapshot()`：读取最近对话、运行状态和 pending interaction。
- 当前 session 的 `prompt()`：把工作台请求加入同一主对话。
- `snapshot.pending` 中的 question wait：在工作台内提交结构化答案。

旧的 `postMessage` 协议保留为兼容兜底，仅当正式 Session 能力不可用时启用，不再作为默认链路。

## 数据与责任边界

### 主对话上下文

插件只镜像有限的主 Session 摘要，不创建第二套会话。摘要包含：

- 当前 `sessionId`；
- 最近 10 个有文本的 Session 节点，包括用户、助手、工具和系统节点；
- 运行时暴露的摘要、目标或任务信息；
- 当前运行状态；
- 当前待处理交互；
- 简历 Markdown、模板 ID、排版测量结果。

这些内容用于让用户知道 AI 正在依据什么，不会写入模板或 `resume.md`。

### 请求

工作台发送的内容会以普通用户消息进入当前 Session，并明确标记工作台上下文：

```text
[DSH_RESUME_WORKBENCH]
请处理当前简历工作台请求：把项目经历压缩两行，保留技术成果。

当前简历：companies/demo/resume.md
当前模板：campus-standard
当前排版：1 页，留白 8%，无溢出
规则：不编造经历；如需改稿先修改工作区文件，完成后说明改动。
[/DSH_RESUME_WORKBENCH]
```

主对话仍然由 Harness Agent 自己决定是否读取文件、调用 `jobhunt_*` 工具、询问用户或调整模板。

### 回答

插件监听同一 Session 的快照更新：

- 新的 assistant message 出现后镜像到 AI 面板；
- 如果回答中包含候选 Markdown，放入“可应用修改”草稿卡；
- 不自动保存文件；
- 主会话结束或当前请求完成后，状态从“处理中”变为“已同步”。

## AI 提问卡片

Harness 的 question wait 本身就是结构化数据。插件直接复用它，不重新设计一套和主对话不兼容的提问协议。

支持：

- 单选；
- 多选；
- 自定义答案；
- 跳过；
- 多个问题的上一题/下一题；
- 回答后继续同一 Session。

回答格式遵循 Harness 的 `QuestionResponsePayload`，由 pending wait 自己负责 request ID 和传输封装。插件只负责视觉呈现和收集答案。

## 安全与体验约束

- 只使用当前主 Session，不创建隐形新会话。
- 发送前显示“将发送到当前主对话”。
- 只同步最近内容，避免把整段历史重复塞入请求。
- 用户问题卡片优先级高于普通聊天输入，避免 AI 已经等待回答时继续发送新请求。
- 每个请求绑定 `requestId` 和 `sessionId`，切换主 Session 后旧请求失效。
- 主 Session 请求不设固定超时；长任务保持真实的“等待中”，同时提供用户主动停止等待入口，并区分 Session 切换、桥接失败和正常完成。
- AI 可以改稿，但保存仍然是用户动作。
- 正式 Session 链路失败时明确显示原因，并保留“复制任务到主对话”兜底。

## 验收标准

### 主会话桥接

- 工作台能显示当前主对话名称/Session 状态。
- 从工作台发送的请求能出现在主对话记录中。
- AI 的最终回答能镜像回工作台。
- 不需要用户切换到主对话页面。
- 当前 Session 不可用时，能降级复制任务而不是假装成功。

### 问题卡片

- AI 提问后工作台出现“需要你确认”。
- 单选、多选和自定义答案可以在工作台内完成。
- 提交后问题消失，主对话继续执行。
- 发送过程中按钮禁用，避免重复回答。

### 简历工作流

- AI 返回的 Markdown 只进入草稿。
- 用户点击“应用到编辑器”后才更新左侧内容。
- 预览继续实时刷新。
- “保存”仍是唯一写回磁盘的动作。

## 实施顺序

1. 插件端接入 `ctx.sessions`，读取当前 Session 和对话快照。
2. 将工作台发送改为当前 Session 的 `prompt()`。
3. 监听 assistant 快照，镜像最终回答并解析候选 Markdown。
4. 在 AI 面板内渲染 Harness pending question。
5. 接通回答提交、状态提示和 fallback。
6. 在本地 Web 中验证：发送、回答问题、候选应用、保存和断开降级。

## 结论

AI 助手不是插件里另起一个模型，而是当前主对话在简历工作台里的一个“就地工作面”。插件提供简历上下文、视觉指标和确认式编辑；Harness 主 Session 提供上下文、Agent、工具和问题处理能力。两者职责清晰，链路可维护，也避免了依赖 DOM 或复制主对话实现。
