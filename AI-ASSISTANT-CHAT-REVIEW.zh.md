# AI 助手聊天流对接复盘

## 目标

把简历工作台里的 AI 助手从“功能侧栏”升级为一个轻量、可回看的工作流聊天窗口。用户应当能在同一个窗口里看到：

- 自己发出的任务；
- 主对话的阶段性回复；
- Think、Read、Edit、Grep、Shell、Tool call 等真实执行事件；
- 需要用户选择的 Question；
- 最终结果和可应用的 Markdown 修改。

主对话仍是唯一的执行入口。插件不新建 Agent、不复制主对话 DOM，也不伪造“正在处理”的步骤，只把 Session 已经产生的可展示节点归一化后重新渲染。

## 当前问题

目前 AI 助手的结构是：上下文状态卡片 + 时间线卡片 + 消息列表 + 输入区。它能工作，但视觉上像调试面板：

1. 工具事件和聊天消息是两个互不相干的区域；
2. assistant 回复是纯文本，长 Markdown 缺少标题、列表和代码层级；
3. 工具执行状态只有颜色和文字，缺少可折叠的事件行；
4. 侧栏内容密度不稳定，长回复会把输入区挤到很下面；
5. 没有明确区分“正在执行”“等待用户”“已完成”“失败”；
6. 用户滚动查看旧记录时，没有可靠的回到底部提示。

## 对接模型

### 统一事件模型

主 Session 的节点先归一化为：

```text
{
  seq,
  type: think | read | edit | grep | shell | tool | question | assistant,
  label,
  target,
  summary,
  status: running | waiting | done | error
}
```

消息和工具事件最终合并成一个按 `seq` 排序的 `feed`，不再分别维护 `messages` 和 `timeline` 两条流：

```text
{
  seq,
  role: user | assistant | event,
  type: user | assistant | think | read | edit | grep | shell | tool | question,
  label,
  target,
  summary,
  status
}
```

渲染层只消费这个稳定结构，不依赖主对话具体 DOM。未来主 Session 增加新的工具类型时，可以只扩展归一化映射和图标，不重写聊天窗口。

### 聊天流顺序

```text
当前 Session 状态
↓
用户消息
↓
Think / 工具事件（可折叠）
↓
assistant 阶段回复（Markdown）
↓
Question 选择卡 / 可应用修改
↓
固定输入区
```

聊天流默认展示当前 Session 的最新一段历史；发起新请求时也不切断旧记录，新的用户消息、Think、工具调用和阶段性回复继续按 `seq` 向下追加。这样用户看到的是一条连续的主对话镜像，而不是每次打开都被重置的“本次请求日志”。

## 视觉与交互原则

### 1. 消息和事件分层

- 用户消息使用右侧浅色气泡；
- assistant 回复使用白底内容块；
- 工具调用使用更紧凑的事件行；
- Think 默认折叠并弱化，避免把内部推理变成聊天正文；
- Question 使用明确的操作卡片；
- 失败状态必须有可读错误，而不是只变红。

### 2. 复用 Harness 原生任务行

插件已接入宿主提供的 `@deepseek-ai/dsh-client-ui-primitives`，任务事件使用 `DisclosureRow` 和宿主原生图标，而不是继续实现一套仿制的折叠卡片。这样可以沿用 Harness 的行高、箭头、展开语义和状态表现；插件只负责把当前 Session 节点映射成任务行数据。

主对话的完整消息 DOM 不直接搬进插件，因为那会依赖宿主内部哈希 class 和私有组件结构，升级后容易失效。可复用的边界是：同源 Session 数据 + 宿主公开 primitives + 相同的事件顺序和信息密度。

### 3. Markdown 安全渲染

第一阶段实现安全的轻量 Markdown 子集：标题、段落、列表、粗体、行内代码和代码块，不使用 `innerHTML`，避免主对话内容直接注入插件 DOM。后续如果宿主已提供可信 Markdown renderer，再替换渲染器，不改变消息模型。

长内容不在数据层静默截断。feed 保留完整节点文本，消息卡片默认只渲染短预览，并提供“展开全文”；工具行继续使用原生折叠，展开后查看完整参数、路径和结果。这样既不丢失 `companies/frontend/preview.html` 这类关键路径，也不会让长会话一次性撑爆侧栏。

### 4. 固定输入区和滚动

聊天窗口使用 `flex` 布局：中间消息流独立滚动，底部输入区始终可见。新消息到达时：

- 用户接近底部：自动跟随；
- 用户正在查看历史：不抢滚动，显示“有新进度”提示；
- 点击提示后回到底部。

### 5. 历史上下文分页

历史记录采用“最新优先进入视口、向上加载更早”的方式：

- 打开助手后默认滚到当前 Session 的最新消息；
- 初始只渲染最近 24 个 feed 节点，避免长会话一次性撑爆侧栏；
- 当更早记录存在时，在消息流顶部显示“加载更早”；
- 点击后追加 24 个更早节点，并用加载前的 `scrollHeight` 差值恢复 `scrollTop`，用户仍停留在刚才阅读的节点附近；
- 新进度到达时，如果用户不在底部，不改变阅读位置，只显示“有新进度，回到底部”；
- 第一阶段不引入虚拟列表。只有当真实 Session 达到数百或上千节点并出现性能数据后，再考虑窗口化渲染。

这里的“分页”只是渲染窗口，不截断主对话数据：归一化 feed 保留完整节点文本，消息正文按需展开，工具行按需展开详情。这样既能回看历史，也不会丢失文件路径、参数和结果。

### 6. 不复制大型 UI 框架

当前插件运行在 DSH 宿主 React 环境中。引入完整 UI 框架会增加打包、样式隔离和宿主版本兼容风险。本轮采用 React 组件 + 局部 CSS 变量和状态类，复用宿主 React，不绑定具体第三方视觉品牌。

## 开发计划

### P0：本轮

- 将时间线改为聊天流中的工具事件组；
- assistant 回复支持安全 Markdown 子集；
- 工具事件支持折叠、状态图标和摘要；
- 调整侧栏宽度、层级、间距和输入区固定布局；
- 历史记录默认落在最新消息，向上按 24 条加载更早节点并保持阅读位置；
- 保留现有 Session、Question、应用 Markdown 修改和请求隔离能力。

### P1：下一轮

- 增加流式阶段性回复；
- 工具事件展开详情，支持定位文件或 Markdown 片段；
- 增加取消当前主 Session 请求；
- 做长回复、连续工具调用和窄屏布局的视觉回归。

## 验收标准

- AI 执行期间，用户能在一个连续聊天流中看到真实工具事件；
- 工具事件不会和 assistant 文本重复显示；
- Markdown 标题、列表、代码块有基本层级；
- 输入区不因长消息消失；
- Question、失败和完成状态能被一眼区分；
- 不展示完整内部思维链，只展示短摘要；
- 打开助手默认位于最新消息，向上加载更早记录后阅读位置稳定；
- 切换简历任务或 Session 后，不串入上一任务的聊天进度；
- 不依赖外部 UI 框架，不改变主对话的执行链路。
