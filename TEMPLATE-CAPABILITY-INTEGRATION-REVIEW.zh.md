# 模板能力对接复盘：从“换样式”到“可生成、可复核的简历模板”

## 1. 结论

`dsh-resume` 要平替的不是某个项目的代码、模板文件或语法，而是用户真正想要的结果：

> 用户描述求职场景和审美偏好，AI 生成一个可解释、可预览、可调整、能稳定落到 A4 的简历模板；用户确认后再进入模板库，并能继续复制、版本化和撤销。

当前版本已经具备模板 Schema、单栏/双栏 Renderer、浏览器 A4 测量、手动微调、模板版本和预览刷新，但 AI 生成模板的入口还偏底层：模型需要自己拼接完整 JSON，设计意图、内容结构、视觉 token 和排版约束没有分层。因此本轮先补齐“设计简报 → 候选模板 → 校验 → 渲染测量 → 用户确认保存”的主链路。

## 2. 参考调研与独立边界

### 可以学习的产品能力

- CodeCV 展示了模板缩略图、模板分类、编辑器与预览并置、样式即时调整、导出和模板复用这些成熟工作流。
- JSON Resume 证明了“内容 Schema 与主题/Renderer 分离”更容易扩展，也更适合让不同模板复用同一份简历内容。
- RenderCV 展示了“结构化设计文件 + 类型化模块 + 可扩展主题 + 多格式输出”的工程路线。
- FlowCV 体现了“先选方向，再填内容，再调设计，最后导出”的低门槛流程。

### 本项目明确不复制的内容

- 不复制 CodeCV 的源码、CSS、模板文件、DOM 结构、图标、图片、品牌文案或精确视觉参数。
- 不把参考项目的模板名称、内部字段、类名或实现细节直接搬进本项目。
- 自己设计 `TemplateSpec`、Design Brief、模块类型、Renderer、A4 测量协议和模板工坊交互。
- 只借鉴公开可观察的产品问题和通用交互模式，所有实现从本项目的用户目标和约束重新推导。

## 3. 当前能力复盘

### 已有基础

- `TemplateSpec`：版式、字号、行高、间距、颜色、字体、密度、单/双栏和模块顺序。
- `layout.json`：区域、模块类型和内容来源，可表达 `timeline`、`skill-tags`、`metric-row`、`portfolio-card` 等扩展块。
- Renderer：独立 Markdown 解析、语义模块包装、A4 页面拆分和浏览器真实测量。
- 工具链：模板列表、校验、保存、复制、版本、恢复、布局保存、自动调优和渲染。

### 现阶段缺口

1. AI 没有一个稳定的“我想要什么风格/给谁投/内容密度如何”的输入层。
2. 模块类型虽已登记，但部分类型只有 class 标记，缺少对应的基础视觉语义。
3. 生成、保存没有明确分成候选阶段和用户确认阶段，容易造成“AI 直接污染模板库”。
4. 模板质量约束仍然主要在渲染后发现，生成阶段还没有把 A4、留白、断裂和可读性作为一等约束。

## 4. 目标架构

```text
用户目标 / 求职场景 / 审美偏好
              ↓
       DesignBrief（设计简报）
              ↓  AI 只产出结构化候选
       TemplateSpec + LayoutSpec
              ↓
          Schema 校验
              ↓
     Renderer 生成独立预览 HTML
              ↓
      浏览器真实 A4 指标回传
              ↓
     有界修复：最多 2～3 轮
              ↓
     缩略图 / 用户对比 / 确认
              ↓
       保存到模板库并建立版本
```

### 设计原则

- AI 生成“设计意图和结构化参数”，不直接生成任意 HTML/CSS。
- 内容、模板、布局和测量相互独立；换模板不改简历内容。
- 每个模板都必须能解释：适合谁、强调什么、风险是什么、如何调整。
- “一页通过”不是只要不溢出，而是 A4 内同时满足目标页数和合理留白区间。
- 保存是显式确认动作；生成候选、预览和入库分开。

## 5. 本轮 P0 方案

### 5.1 DesignBrief

DesignBrief 是给 AI 的低复杂度输入层，包含：

- `audience`：校招、技术、产品、设计、学术、通用。
- `layout`：单栏或双栏。
- `density`：紧凑、标准、舒展。
- `tone`：清晰、技术、编辑、极简、终端。
- `moduleOrder`：希望突出哪些经历。
- `moduleTypes` / `sidebarModules`：指定技能标签、时间线、项目卡片以及双栏侧栏模块。
- `palette`：可选的强调色、背景色和文字色。
- `bestFor` / `tags`：用于模板库筛选和缩略图说明。

AI 只需要描述意图，生成器负责把它映射为受约束的 `TemplateSpec`。这样可以避免模型每次自由发挥导致字段遗漏、颜色对比不足或范围越界。

### 5.2 模块 Renderer

本轮把已登记的模块类型变成稳定的视觉语义：

- `skill-tags`：列表转为可换行的技能标签。
- `timeline`：保留经历顺序并提供时间线语义 class。
- `metric-row`：为指标行提供紧凑横向布局语义。
- `portfolio-card` / `project-list`：为项目块提供卡片和列表变体入口。
- `qr-code`：保留内容来源，由模板决定尺寸和位置，不在 Renderer 内执行不安全的远程请求。

这些模块仍然复用同一份 Markdown 内容，不要求用户学习新的私有标记语言。

### 5.3 工具链

新增 `jobhunt_template_generate`：

1. 接收 DesignBrief；
2. 返回解释、候选 `TemplateSpec`、`LayoutSpec` 和下一步建议；
3. 不自动写入模板库。

标准调用顺序：

`generate → template_validate/layout_validate → render → layout_metrics → autotune（最多 3 轮）→ 用户确认 → template_save/layout_save`

这样既支持 AI 生成模板，也保留用户对最终版本的控制权。

## 6. P1 / P2 计划

### P1：真正的模板工坊

- 模板候选并排对比和缩略图快照。
- 设计 token 面板：页面底色、标题样式、分隔线、标签、卡片、圆角和侧栏底色。
- 按“内容密度、行业、投递场景、结构”筛选模板。
- 模板草稿、复制、命名、版本差异和撤销。
- 针对单栏/双栏/侧栏比例的可视化调节。

### P2：高阶主题能力

- 可组合的 section/header/list/card token。
- 可选本地头像、二维码和图标资源，全部经过相对路径和尺寸校验。
- 导出前的打印色彩、字体回退和分页一致性检查。
- 模板质量评分：可读性、信息密度、留白、断裂、对比度和可维护性。

## 7. 验收标准

- AI 不提供完整 HTML/CSS 也能生成一个合法模板候选。
- 候选模板通过 Schema 校验，颜色对比度和尺寸范围受控。
- 同一份 Markdown 可切换至少单栏和双栏模板。
- `skill-tags` 等扩展块在预览中有实际视觉差异，而非只改变 class 名。
- 生成不会自动写入模板库；用户确认后才保存并可恢复版本。
- 渲染后能收到浏览器真实 A4 指标，并能继续进行有界调优。
- 新能力不依赖 CodeCV 的代码、模板文件、CSS 或资源。

## 8. 本次实现记录

- 新增独立 `DesignBrief` 归一化和校验模块。
- 新增从 DesignBrief 生成受约束 TemplateSpec 的候选生成器。
- 新增 `jobhunt_template_generate` 工具，明确“候选”和“入库”的边界。
- 为扩展模块补充基础 Renderer 语义和样式。
- 增加生成、校验和模块渲染测试。
