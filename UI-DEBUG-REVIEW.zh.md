# dsh-resume 用户视角 UI 调试复盘与对接方案

> 复盘日期：2026-08-22
> 对接对象：DSH Web + dsh-resume 侧边工作台
> 复盘方式：通过本地 Chrome MCP 实际打开、点击、读取页面和截图；不是只看静态代码。

## 1. 复盘结论

dsh-resume 当前已经具备基本工作台结构，但用户目标还没有被界面明确地组织起来。

用户真正想完成的是：

> 把真实经历整理成一份视觉上可信、内容密度合适、可以确认后投递的简历。

因此主流程不应该是“学习模板、填写 JSON、调整参数”，而应该是：

```text
进入插件 → 选择入口 → 看懂当前问题 → 自动/手动调优 → 确认刚好一页 → 导出
```

本轮优先处理三个问题：

1. 首次入口接口异常时不能只显示 JSON 解析错误；
2. “一页”必须区分“没溢出”和“密度合格”，并告诉用户下一步；
3. 模板库负责选择，模板工坊负责生成和维护，两个页面不能让普通用户面对同一套开发者概念。

## 2. 实际用户路径记录

### 2.1 已通过的路径

- 点击“开始”会停留在开始页，不再自动跳到预览；
- 点击“模板库”会停留在模板库，不会强制跳转；
- 点击“模板工坊”会停留在模板工坊；
- 点击“先看看示例”可以进入预览并生成版式测量；
- 预览以外的页面不再显示右侧“当前状态”面板。

### 2.2 实际发现的问题

#### P0：运行实例/profile 不一致

在 3099 实例中，`/dsh-resume/api/status` 正常，但新增加的
`/dsh-resume/api/onboarding` 返回了 DSH HTML 页面。前端直接调用
`response.json()`，于是显示：

```text
初始化失败：Failed to execute 'json' on 'Response'
```

同一份源码以正确 profile 启动到 3100 后，onboarding 接口返回 JSON，流程正常。
这说明除了前端容错，还需要把安装、profile 和重启后的运行实例统一，否则用户会看到“插件有页面但按钮坏了”。

#### P1：一页但偏空的结果表达不够强

实际示例测量结果为：

```text
1 页
留白约 51%
状态：一页但偏空
```

检测本身是正确的，但预览页面视觉上仍然像“已完成”，用户容易把“不溢出”误认为“排版通过”。

“一页通过”的定义应为：

- 页数为 1；
- 无内容溢出；
- 内容高度达到目标密度；
- 底部保留必要呼吸空间，而不是出现大块空白。

本产品不追求物理意义上的 0 空白，而是追求可读的高密度投递版。建议将目标留白控制在约 4%～10%，并把目标展示给用户。

#### P1：右侧状态面板信息堆叠

预览页右侧同时放置了版式结果、视觉模板、四个滑杆、内容检查、工作区、投递前确认和检查页数量。信息都合理，但优先级相同，用户不知道下一步该做什么。

状态面板应改成：

1. 当前结论；
2. 造成结论的关键指标；
3. 推荐动作；
4. 手动调整（可折叠/次级）；
5. 其他信息（可折叠）。

#### P1：模板工坊仍然像开发者页面

当前页面以 JSON 文本框为主，普通用户无法判断：

- JSON 从哪里来；
- 粘贴后会改变什么；
- 如何预览；
- 保存后如何撤销。

模板工坊需要保留 AI 生成能力，但默认展示“生成 → 预览 → 保存 → 版本”的流程，JSON 编辑应降级为高级入口。

#### P1：模板库的选择依据不足

模板名称和岗位标签存在，但用户还需要直接看到：

- 单栏还是双栏；
- 内容密度；
- 适合岗位；
- 视觉关键词；
- 当前模板是否会导致留白或溢出。

缩略图不只是装饰，而是模板库的主要决策信息。

#### P2：开始页缺少推荐路径

“我已有简历 / 从零开始 / 先看看示例”三个入口是合理的，但没有告诉新用户哪个最适合第一次使用。

建议把“先看看示例”标为推荐入口，并让错误状态提供重试，而不是只显示异常字符串。

## 3. 本轮改动范围

### P0

- 增加 onboarding 响应容错：检查响应类型，非 JSON 时给出“插件服务未刷新/请重启 Web”的可理解提示；
- 增加重试入口，保留用户当前路径；
- 在文档和启动脚本说明中明确 profile 与运行实例必须一致。

### P1

- 将右侧版式结果改为“结论 + 指标 + 推荐动作”；
- 把“手动调整”标记为次级操作，避免与版式结论抢注意力；
- 模板库卡片增加密度、布局和用途信息；
- 模板工坊增加三步引导和当前模板预览，JSON 编辑改为高级区域；
- 明确“应用并查看预览”是模板库到预览的唯一确认动作。

### P2

- 开始页标注推荐入口；
- 统一“投递版本 / 预览 / 模板库 / 模板工坊”的文案层级；
- 排版检查页展示同一套留白、溢出和模块断裂指标；
- 后续接入 2～3 轮自动调优按钮，并在每轮显示调整原因。

## 4. 验收标准

### 首次使用

- 新用户打开插件能理解三个入口的区别；
- 点击任一入口失败时，看到可执行的重试/重启提示；
- 不出现 `Response.json()`、HTTP 状态码或内部路径作为主错误文案。

### 导航

- 点击“开始”不会自动跳到预览；
- 点击“模板库”和“模板工坊”不会自动跳到预览；
- 只有点击“应用并查看预览”或明确选择预览导航时才进入预览；
- 右侧状态面板只在预览页显示。

### 一页判定

- `1 页但偏空` 不显示为通过；
- `1 页但有溢出` 不显示为通过；
- 只有一页、无溢出且达到目标密度才显示“一页通过”；
- 状态面板必须同时显示页数、留白比例和推荐动作。

### 模板

- 模板库可以在不离开页面的情况下比较并选择模板；
- 每张模板卡片能看出布局、定位和视觉差异；
- 模板工坊默认先让用户理解流程，JSON 只作为高级编辑方式；
- 保存、复制、版本和恢复后都能给出明确结果。

## 5. 本轮已落地

- 新增 `readJsonResponse`：首次入口遇到 HTML/非 JSON 响应时，显示“服务尚未刷新，请重启 DSH Web 后重试”，并提供重试按钮；
- 开始页将“先看看示例”标记为第一次使用的推荐入口；
- 模板库卡片补充单栏/双栏、岗位标签和视觉描述，并保留“应用并查看预览”的确认动作；
- 模板工坊改为三步流程说明，增加当前模板小预览，JSON 编辑降为高级区域；
- 预览右侧改为“版式结论 → 下一步 → 当前模板 → 手动微调 → 更多状态”的优先级；
- 一页密度阈值从 12% 收紧为 10%，预览状态始终显示页数和留白比例；
- 已通过 MCP 回归“开始、模板库、模板工坊、先看看示例、预览测量”路径。

## 6. 后续迭代顺序

1. 先修运行实例和 onboarding 错误反馈；
2. 再改右侧状态面板和“一页通过”表达；
3. 再改模板库与模板工坊；
4. 最后接入基于真实测量结果的 2～3 轮自动调优；
5. 每完成一阶段，都用 MCP 重新点击一次完整用户路径。
