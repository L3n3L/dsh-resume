# 模板资产重做对接复盘

## 1. 本轮目标

本轮目标不是继续堆 Renderer 数量，而是把已有模板从“结构能跑、CSS 很薄”提升为“每套模板都有完整视觉系统”。最终用户应该能根据求职场景和个人气质，直接选到一份视觉方向明确、内容可读、能稳定落在 A4 页面内的简历。

验收标准：

- 五套重点模板拥有明显不同的头部、模块标题、条目、技能和时间线表达；
- 使用同一份饱满的林知远示例简历时，缩略图能看出真实内容和视觉差异；
- 不依赖图片、外链字体或外部资源也能完整渲染；
- 手动字号、行高、模块间距、页边距仍然有效；
- A4 页面尺寸、分页和测量契约不被模板 CSS 破坏；
- 模板 CSS 只负责视觉表达，不改变语义顺序和内容事实。

杂志类模板的每个故事条目必须是分页器可以单独识别的直接布局单元，不能把整列内容包成一个不可拆分的大容器。

## 2. 研究结论

### CodeCV 借鉴点

对 `E:\vsws\codecv-master` 的本地代码、模板入口和模板缩略图进行了查看。值得借鉴的是产品组织方式，不是复制其代码：

1. 模板库按“校招、社招、英文、Geek、运营、商务、设计、互联网、简约、暗黑”等使用场景分类；
2. 每个模板卡片都展示真实内容缩略图，用户先看结果，再决定是否进入编辑；
3. 模板之间不只换主题色，而是改变头部比例、信息密度、模块节奏、边框和内容重心；
4. 模板可以通过颜色、背景、字体和行高做快速微调，适合和 DSH 的 Token 面板结合。

我们不复制 CodeCV 的模板代码、类名、DOM、图标字体或具体装饰，而是使用已有的 DSH Renderer、Layout IR 和独立 CSS 管道重新实现。

### 外部设计研究结论

- Figma 的简历设计建议强调留白、层级、对齐、有限强调色和清晰的单/双栏职责分工；
- American Chemical Society 建议保持清晰组织、适当页边距、可读字号、短要点和有限字体数量；
- Canva 的视觉层级方法强调尺寸、颜色对比、字体层级、间距和构图共同建立阅读路径；
- ATS 兼容性意味着关键信息必须保持真实文本和明确标题。双栏、照片、图形化技能条只能作为特定场景模板，不应成为默认模板。

参考：

- https://www.figma.com/resource-library/resume-design-ideas/
- https://www.acs.org/careers/career-services/career-pathways/industry/resume/design-and-format.html
- https://www.canva.com/learn/visual-hierarchy/

## 3. 五套重点模板设计决策

| 模板 | 场景 | 视觉主张 | 主要设计语法 | 风险控制 |
| --- | --- | --- | --- | --- |
| `campus-standard` | 校招 / ATS | 清晰、可信、稳妥 | 蓝色细线、代码式章节前缀、对齐日期、低装饰 | 单栏、文本优先 |
| `portrait-profile` | 设计 / 运营 / 个人品牌 | 先记住人，再读经历 | 头像锚点、圆角信息卡、品牌色标签、左右节奏 | 头像缺失时自动占位，不影响内容 |
| `geek-lab` | 工程 / 测试 / 基础设施 | 像一个可运行的工程系统 | 深色终端、模块编号、等宽字体、状态标签、命令提示 | 保持高对比，避免只靠颜色表达 |
| `magazine-feature` | 产品 / 内容 / 品牌 / 运营 | 把最强故事放到首屏 | 编辑部眉题、衬线标题、主次栏、引导线、短段落 | 正文仍使用清晰无衬线，控制行宽 |
| `case-study` | 产品 / 设计 / 作品集 | 先看成果，再看证据 | 首个项目主卡、结果标签、证据栏、边框和投影 | 重点卡不吞掉全部内容，保持可分页 |

## 4. CSS 资产契约

模板继续采用：

```text
templates/<id>.json   // Token、Renderer、Layout IR、描述
templates/<id>.css    // 该模板完整视觉层
```

CSS 允许使用已有的语义类和 Renderer 类，例如 `.dsh-resume-section`、`.dsh-renderer-item`、`.dsh-portrait-head`、`.dsh-case-feature`，但不得改写内容排序、分页脚本或指标采集逻辑。

注入顺序保持：

```text
default.css
→ 框架默认视觉
→ 模板独立 CSS
→ AI customCss
→ 手动 Token
→ A4 / 分页契约
```

模板 CSS 的目标体量是“完整但克制”，不是为了达到某个字节数而堆规则。每套模板至少覆盖：

- 页面背景、纸张和边界；
- 姓名、联系方式、摘要和头像的首屏关系；
- 一级模块标题和模块间距；
- 教育、项目、实习条目的标题、日期、正文和要点；
- 技能标签或技能组；
- 链接、强调文本、引用、表格等 Markdown 内容的降级表现；
- 打印场景和窄屏预览的可读性。

## 5. AI 生成模板的边界

AI 可以生成 `TemplateSpec`、Layout IR 和独立 `templateCss`，但 CSS 仍需经过安全校验、长度限制、同源资源限制和浏览器预览验收。AI 负责表达设计意图，插件负责：

1. 校验 Token 和结构；
2. 编译/注入 CSS；
3. 绑定 A4 页面契约；
4. 采集真实分页、溢出和留白指标；
5. 在不合格时给出可操作的调整建议。

## 6. 本轮实现与验收

本轮只改五套重点模板的视觉资产和必要测试，不重写 Renderer，不复制 CodeCV 资源。每套模板完成后使用同一份真实感示例简历生成缩略图，并检查：

- 五张缩略图是否一眼可区分；
- 姓名、教育、技能、项目和实习是否真实出现在缩略图中；
- 是否出现空白模板、内容截断、异常滚动条或深色文字对比不足；
- 是否仍保持 794×1123 的 A4 页面；
- 是否能通过现有自动化测试和浏览器回归。

## 7. 不纳入本轮的事项

- 不复制 CodeCV 的具体模板、图标字体、图片或 CSS；
- 不新增未经验证的复杂布局引擎；
- 不用装饰性图形替代真实文字内容；
- 不为了填满一页强行缩小到不可读字号；
- 不把所有模板都做成双栏或暗色，避免模板库只是换颜色。
