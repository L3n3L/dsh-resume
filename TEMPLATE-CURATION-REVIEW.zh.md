# 模板收敛与正文视觉层对接复盘

## 结论

模板库不再以“预设数量”作为丰富度指标，而以“用户能否一眼选出适合自己的完整视觉方向”为指标。

本轮默认图库保留 6 套方向：

| 模板 | 目标场景 | 保留理由 |
| --- | --- | --- |
| 校招标准 | 校招、ATS、通用投递 | 信息密度和可读性基准 |
| 肖像侧栏 | 设计、运营、个人品牌 | 头像和身份锚点 |
| 杂志开篇 | 内容、品牌、运营、产品 | 编辑式主叙事与栏流 |
| 极客实验室 | Geek、开发、工程 | 深色终端和模块化节奏 |
| 重点案例 | 产品、设计、作品集 | 重点项目和证据层级 |
| 商务履历增强 | 社招、业务、项目成果 | 深色信息头、时间线和成果密度 |

相似或未完成的旧内置模板已经从 preset 源码中删除，不再注册、不再进入模板库。旧工作区里的用户自定义文件不会被物理删除，但非 composition 模板会被运行时过滤，避免破坏用户已有文件。后续只有完成独立视觉系统、真实简历验收和缩略图验收的模板，才允许加入默认图库。

## 本轮发现

之前的问题不是 CSS 文件字节数不够，而是两层契约不完整：

1. Markdown 输出中的项目标题、日期、要点仍主要依赖通用 HTML 标签，模板无法稳定识别“条目标题”和“条目元信息”。
2. 运行时调优样式在模板 CSS 之后注入，且部分视觉规则使用 `!important`，导致模板对字号、标题颜色和正文层级的设计被压制。

## 正文语义契约

每个带三级标题的经历/项目条目补充以下类：

- `.dsh-entry-title`：项目或岗位标题；
- `.dsh-entry-meta`：日期、地点、技术栈等元信息；
- `.dsh-entry-bullets`：该条目的成果要点列表。

原有 `.dsh-module-*`、`.dsh-renderer-item-*`、布局和分页节点全部保留，保证旧模板和布局 IR 兼容。日期识别只对紧跟三级标题且包含日期/至今/时间分隔符的段落生效，普通描述不会误判。

## 样式优先级

保留 `!important` 的范围：A4 尺寸、分页容器、滚动和测量所需的框架契约。

移除或降低优先级的范围：正文字号、正文颜色、模块标题颜色、条目间距和模板装饰。这样模板 CSS 才能真正控制内容层级；用户手动 Token 仍通过 CSS 变量调节整体字号、行高和间距。

## 验收标准

- 默认图库只有 6 套，不再展示高度相似的白底换色模板；
- 已删除的旧内置模板 ID 不再可加载；用户工作区文件仍不被物理删除；
- 5 类正文节点均有可命中的语义类；
- 6 套模板至少覆盖：ATS 单栏、头像、编辑式、暗色终端、案例导向和商务履历；
- 用林知远的饱满示例简历渲染时，项目标题、日期、要点、链接和技能层级清晰；
- 运行测试全绿，并对保留模板完成真实预览截图检查。

## 后续规则

新增模板必须先回答“它解决了哪种内容组织问题”，再提交 CSS。只改变颜色、圆角或标题装饰的模板不进入默认图库。
