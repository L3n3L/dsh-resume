# 主 Session 统一上下文对接复盘

## 结论

简历工作台里的 AI 助手不应该拥有第二套对话。主 Harness Session 是唯一真实上下文，插件只是它的一个就地工作面：

```text
插件内提问
  → 当前主 Session.prompt()
  → 主对话继续使用原历史、工具和待处理交互
  → 插件监听同一个 Session 的快照
  → 在插件内镜像回答或展示提问卡
```

这样用户在插件内外说话的对象相同。插件只额外携带当前简历、模板、排版指标和选中文本，不创建隐形聊天，不复制一份模型上下文。

## 之前的问题

早期上下文摘要只筛选最近几条 `user/assistant` 文本。它可以完成简单问答，但在长对话里会丢失：

- 工具调用和工具结果；
- 系统节点或工作流状态；
- 主对话暴露的目标、标题或摘要；
- 当前 Session 的待处理交互；
- 用户已经确认过的决策线索。

因此“发送入口统一”不等于“上下文读取完整”。用户在插件里发送的消息会进入主 Session，但插件再次构造工作台请求时，可能只带上不完整的历史摘要。

## 本轮改造

### 1. 统一真实来源

- 使用 `ctx.sessions.currentProvideInfo` 获取当前 Session。
- 使用当前 Session 的 `getSnapshot()` 作为对话状态来源。
- 使用当前 Session 的 `prompt()` 发送工作台请求。
- 插件内 `chatMessages` 只负责镜像显示，不作为第二个会话源。

### 2. 扩展 Session 摘要

摘要现在保留最近十个有文本的节点，并记录：

- `role`：用户、主对话 AI 或系统节点；
- `kind`：原始节点类型，便于识别工具/系统节点；
- `text`：节点中的文本、结果、错误或输出；
- `seq`：用于关联请求前后的回复；
- `running`：当前主对话是否仍在运行；
- `pending`：当前待处理交互；
- `sessionFacts`：运行时暴露的摘要、目标、任务或标题。

摘要不是完整数据库导出，而是发给 Agent 的有限上下文窗口，避免把整段历史和隐私无上限重复发送。

### 3. 请求生命周期

每个工作台请求都绑定：

```text
requestId + sessionId + baselineSeq + transport
```

规则：

- 主 Session 切换后，旧请求立即失效；
- 主 Session 无结果超过 45 秒，显示失败而不是永久等待；
- 旧的跨窗口桥接 3.2 秒没有响应时进入复制任务兜底；
- 桥接事件必须处于当前请求生命周期内，并优先匹配 `requestId`；
- 编辑器重新打开时清理旧请求，避免旧回答落入新模板；
- 主对话有待处理问题时，禁用插件继续发送。

## 插件内外的用户体验

### 在插件内说话

插件会把请求作为带有 `[DSH_RESUME_WORKBENCH]` 标记的用户消息送入当前主 Session。主对话仍然可以读取文件、调用 `jobhunt_*` 工具、提问和修改草稿。

### 在外部主对话说话

主对话直接使用同一个 Session，因此可以看到插件发送的消息和 Agent 的后续结果。插件不保证永久保存自己的聊天面板缓存，但主 Session 才是可持续的记录。

### 两者的区别

区别只在展示和附加上下文：

- 外部主对话拥有完整原生 UI；
- 插件显示与简历相关的镜像回答、排版指标和候选 Markdown；
- 插件发送时会附带当前简历工作台状态；
- 两者不应该拥有不同的模型或不同的真实会话。

## 不自动做的事情

- 不自动保存 AI 返回的 Markdown；
- 不把插件聊天缓存当成长期记忆；
- 不在主 Session 之外创建隐形线程；
- 不凭自然语言猜测用户已经确认的内容；
- 不在 Session 切换后继续监听旧请求。

## 验收标准

### 连续性

- 在外部主对话确定“使用双栏模板”；
- 在插件内发送“把技能放到右侧”；
- 主对话能看到同一条请求，并继续使用先前决定；
- 插件能镜像最终回答；
- 离开插件后，外部主对话仍可继续讨论这次修改。

### 完整性

- 工具节点不会被摘要过滤掉；
- 主 Session 的摘要/目标可进入工作台请求；
- 结构化提问期间不能再发第二个工作台请求；
- 切换 Session 后旧回答不会出现在新 Session 的编辑器里。

### 异常恢复

- 主对话拒绝请求时显示明确原因；
- 主对话超时时可复制任务到主对话；
- 事件桥接收到旧 `requestId` 时不会污染当前面板；
- 重新打开编辑器后不会继承上一份请求的等待状态。

## 后续演进

如果运行时以后提供稳定的 Session summary、turn id、tool trace 或 durable memory，应优先接入这些正式字段，而不是继续扩大客户端对快照内部结构的猜测。当前客户端对未知节点采取有限文本提取，属于兼容层，不替代主 Session 的正式上下文 API。

