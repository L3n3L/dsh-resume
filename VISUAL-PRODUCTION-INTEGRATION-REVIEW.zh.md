# 视觉生产闭环对接复盘：让 DSH 负责产出好简历

## 1. 产品核心目的

`dsh-resume` 不是独立的模板编辑器，也不是 Markdown 预览器。它是 DSH 的简历视觉生产插件：

> 用户提供真实经历和目标岗位，Harness/Agent 负责把内容整理成有竞争力的简历，并完成模板选择、视觉设计、A4 排版、测量复核和投递版本管理；用户只在关键节点确认结果。

产品结果同时满足两条线：

- **文字结果**：经历真实、重点清楚、与目标 JD 有关、项目结果可验证；
- **视觉结果**：层级清楚、密度平衡、A4 分页稳定、可读且适合投递。

模板、Token、Markdown 编辑器都只是实现手段，不能让用户承担设计和排版工作。

## 2. DSH 插件边界

本项目必须保持 DSH 插件形态：

- AI 通过 Harness 主对话、插件工具和系统提示词工作；
- 插件客户端通过 DSH slot 注入工作台、侧栏入口和 Settings 面板；
- 预览页只负责渲染和回传真实浏览器测量数据；
- `jobhunt_layout_metrics` 是 Agent 获取视觉事实的正式入口；
- 用户在插件内的 AI 助手是主对话镜像/桥接，不另建一套 AI 服务；
- 不新增独立后端，不在前端保存 API Key，不绕过 DSH 任务流和确认机制。

数据流：

```text
用户目标 / 真实经历 / JD
        ↓
Harness 主对话与工具调用
        ↓
内容整理 + TemplateSpec + LayoutSpec
        ↓
dsh-resume Renderer 输出 A4 预览
        ↓
浏览器回传页数、留白、模块和视觉平衡指标
        ↓
Harness 根据事实进行有限轮次视觉调优
        ↓
用户确认 → 保存投递版本 / 导出
```

## 3. 当前问题复盘

### 3.1 文字侧

- AI 可以改 Markdown，但缺少稳定的“岗位目标 → 内容重点 → 视觉重点”联动；
- 过长内容容易通过缩小字号硬塞进一页；
- 模板切换、内容修改和排版调整的职责边界需要继续保持清楚。

### 3.2 视觉侧

- 当前指标主要回答“几页、溢出、留白多少”，还没有充分回答“页面是否平衡”；
- 现在的模板差异已经包含单栏、双栏、时间线、标签和卡片，但模块级视觉语义仍需丰富；
- 用户可以手动调 Token，但更理想的交互是直接对 AI 说“项目更突出”“底部不要空”“正式一点”；
- “一页通过”不能等同于“排版完成”，还要检查视觉重心、模块断裂和可读性。

## 4. AI 与插件的职责分工

### Agent / Harness 负责

- 读取真实材料和目标 JD；
- 判断内容优先级、模块顺序和模板家族；
- 生成或选择 TemplateSpec、LayoutSpec；
- 调用渲染和 `jobhunt_layout_metrics`；
- 根据测量事实提出有限轮次调整；
- 解释调整原因，并等待用户确认后保存。

### 插件负责

- 提供稳定的模板家族、语义模块和安全视觉 Token；
- 把 Markdown 渲染成固定 A4 页面；
- 回传结构化视觉指标，不凭感觉声称“一页通过”；
- 展示 AI 的修改前后差异、当前预览和用户可控的撤销入口；
- 防止 AI 修改内容时丢失用户未保存草稿。

## 5. P0 视觉升级

### 5.1 页面平衡指标

在现有 A4 指标上补充：

- 首页和末页内容占用率；
- 页面顶部、底部留白；
- 模块数量和模块高度分布；
- 模块是否跨页或出现孤立标题；
- 页面视觉重心：顶部偏重、底部偏空、整体平衡；
- 可读性风险：字号过小、行高过紧、正文密度过高。

指标必须从浏览器真实 DOM 测量产生，并随 `jobhunt_layout_metrics` 回传给 Agent。

### 5.2 模块级视觉语义

优先支持：

- 项目成果指标；
- 技能标签；
- 经历时间线；
- 项目卡片；
- 侧栏信息块；
- 顶部全宽个人信息区；
- keep-together 分页提示。

模板变化应来自结构和模块表现，而不是只改颜色。

### 5.3 一句话视觉调优

在 AI 助手中提供快捷意图，但仍桥接主对话：

- “让项目经历更突出”；
- “减少底部留白，不要缩小字号”；
- “改成正式、适合国企的视觉”；
- “保持一页，增加信息层次”；
- “降低装饰，优先 ATS 可读性”。

插件把当前 Markdown、模板、LayoutSpec 和最新指标作为上下文交给 Harness，不能在客户端自行伪造 AI 结论。

## 6. 调优规则

- 优先调整模块顺序、重复内容和低信号要点；
- 其次调整 section gap、paragraph gap 和 page margin；
- 最后才在可读范围内调整字号和行高；
- 每轮都必须重新渲染并读取真实指标；
- 默认最多 3 轮，用户可以继续要求，但不能静默无限循环；
- 不得为了“一页”牺牲真实经历、可读性或 ATS 解析。

## 7. P1 / P2 路线

### P1：视觉决策自动化

- 岗位风格预设：大厂技术、正式商务、产品运营、学术研究、英文技术；
- AI 视觉审计卡片和前后对比；
- 模块级拖动/上移建议，但默认由 AI 完成；
- 头像、二维码、作品链接和个人品牌区；
- 顶部全宽 + 下方双栏、多栏混合结构。

### P2：质量与生态

- A4 页面热力图和视觉重心可视化；
- PDF 与浏览器截图差异检测；
- 模板版本、评分和用户反馈闭环；
- 第三方原创模板导入和安全校验；
- 面向不同国家/语言的字体和排版规则。

## 8. 本轮验收标准

- `jobhunt_layout_metrics` 能返回页面平衡相关数据；
- 页面检查能够区分“一页通过”“一页偏空”“模块断裂”和“多页”；
- AI 能基于真实指标提出视觉建议，而不是只说“调整一下”；
- 快捷视觉意图仍然进入 Harness 主对话；
- 内容、模板和排版参数仍然分离保存；
- 用户可撤销视觉调整并恢复模板默认值；
- 不新增独立 AI 后端，不复制 CodeCV 的源码、模板或视觉资产；
- 所有现有测试继续通过。

