# Layout IR 与分页协同提测报告

## 测试范围

- 代码单测、Markdown/模板渲染、旧布局兼容。
- 真实浏览器 `http://localhost:3100/dsh-resume/`。
- 桌面视口：`1440 × 1000`。
- 真实工作区：秋招 / `companies/frontend/preview.html`，使用林知远（较饱满）简历内容。

## 自动化结果

| 检查项 | 结果 |
| --- | --- |
| `npm test` | 25 / 25 通过 |
| `git diff --check` | 通过 |
| `npm pack --dry-run` | 通过，包含 Layout IR 对接文档 |
| 插件安装 | 成功；仅保留既有 `@deepseek-ai/cordis` peer warning |

新增回归覆盖：

- 旧 `mode + regions` 自动迁移为 `split`。
- 显式 `grid` 的列数、间距和顺序保留。
- IR 顺序真正影响模块顺序和 Renderer 容器。
- AI 生成的作品集候选直接生成 `grid` IR。

## 浏览器验收

### 侧栏信号 / split

- 工作台成功切换到“侧栏信号”。
- A4 头部、主栏、深色侧栏均可见，技能模块进入侧栏，教育经历进入主栏。
- 当前内容结果：`留白 34% · 3 页`。
- 页面没有看到横向截断、模块被吞掉或脚本错误；分页按模块推进。

![侧栏 IR 真实浏览器验收](/E:/vsws/deepseek-harness-plugins/1-插件源码/dsh-campus-job/docs/screenshots/layout-ir-split-browser.png)

### 作品集网格 / grid

- 工作台成功切换到“作品集网格”。
- 头部、教育、技能和项目卡片均正常渲染，项目模块出现卡片边界。
- 当前内容结果：`留白 22% · 2 页`。
- 没有看到内容截断或溢出标记；只有一个项目模块时，项目卡片占满网格行是预期行为。

![网格 IR 真实浏览器验收](/E:/vsws/deepseek-harness-plugins/1-插件源码/dsh-campus-job/docs/screenshots/layout-ir-grid-browser.png)

### 刷新回归

- 浏览器刷新后 Harness 页面可恢复，重新进入“求职简历”工作台不崩溃。
- 未点击“保存”的模板选择回到默认“校招标准”，符合当前“应用后草稿态、保存后持久化”的行为；这不是本阶段分页缺陷。

## 已发现的体验边界

### P2：模块级分页可能产生较大底部留白

侧栏信号在当前饱满简历上为 3 页，第一页主栏在教育模块后保留较明显空间。原因是本阶段把完整模块作为分页原子，不把长项目模块拆成列表项；这样优先保证内容不被截断，但距离“尽量刚好一页/少页”仍有优化空间。

后续建议：增加受控的 `li` 级拆分策略，并在拆分前保留标题与至少一条要点，避免为了填满页面破坏模块语义。

### P2：网格的视觉差异取决于模块数量

当前示例只有一个项目模块，因此网格不能展示多个项目卡片的并列效果。真实内容包含多个项目模块或未来支持 `project-item` 级 IR 后，网格价值会更明显；不是 Renderer 未生效。

## 提测结论

本阶段达到提测条件：核心 IR、旧布局兼容、分页协同和浏览器渲染均通过；暂不阻塞合并。建议把两个 P2 作为后续“细粒度分页 / 项目项级布局”的独立迭代，不在本次继续扩大分页改动范围。
