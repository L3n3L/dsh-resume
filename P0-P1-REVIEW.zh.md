# P0/P1 渲染链路对接复盘

## 1. 复盘结论

本轮集中处理三个会直接影响“AI 生成内容能否稳定变成可投递简历”的问题：

| 优先级 | 问题 | 影响 |
| --- | --- | --- |
| P0 | 多会话渲染状态和 A4 指标可能错位 | 不同简历、旧预览和新指标可能互相覆盖，AI 会基于错误数据继续调版 |
| P1 | Markdown 解析能力过弱 | 用户换行、表格、嵌套列表等内容可能被合并或丢失 |
| P1 | 模板没有受控自定义 CSS | AI 只能修改有限 Token，难以生成真正有差异的视觉模板 |

目标不是让 AI 直接接管浏览器，而是让 AI 的内容、模板和测量结果形成可靠闭环：

```text
AI 写入内容/模板
→ 生成唯一 renderId 和 contentHash
→ 浏览器按同一份产物测量
→ metrics 校验 root、previewPath、renderId、contentHash
→ 通过后才交给 AI 做下一轮排版调整
```

## 2. P0：渲染产物与 metrics 防错

### 当前问题

- 默认输出仍可能是同一工作区下的 `preview.html`。
- `previewState` 是进程级单对象，无法同时表达多个工作区和多个预览文件。
- metrics 过去只比较 `previewPath`，同路径不同内容会被误认为同一份预览。
- 状态接口在没有当前状态时从磁盘列表取第一个预览，可能选中旧文件。

### 本轮方案

- 用 `root + previewRel` 作为状态 Map 的稳定键。
- 每次渲染生成 `renderId`，并对 Markdown、CSS、TemplateSpec 和结构化 HTML 输入生成 SHA-256 `contentHash`。
- 将 `renderId`、`contentHash`、`root`、`previewPath` 写入预览文档和浏览器 metrics 消息。
- metrics 必须匹配四项身份信息；不匹配返回 409，不污染当前状态。
- `/api/status` 没有当前状态时返回空状态，不再静默选择 `previews[0]`。
- 预览文件用临时文件加 rename 写入，避免读到半截 HTML。

### 验收标准

1. A 渲染后，B 渲染同一工作区或不同工作区，A 的旧 metrics 不能覆盖 B。
2. 同一个 `previewPath` 但内容不同，旧 hash 上报必须被拒绝。
3. 状态接口不会凭磁盘排序结果自动选中旧预览。
4. 正常打开预览后，metrics 仍能进入对应状态并被 `jobhunt_layout_metrics` 读取。

## 3. P1：Markdown 解析

### 当前问题

原实现只覆盖少量标题、列表和行内格式，并会把连续多行文本合并为空格分隔的单段文本。简历中常见的表格、嵌套列表、任务清单、图片和代码块没有稳定语义。

### 本轮方案

- 使用成熟的 `markdown-it` 解析 Markdown。
- 开启 HTML 转义，关闭原始 HTML 注入；链接统一限制为安全协议。
- 保留简历需要的标题、段落、列表、表格、任务列表、引用、代码块和链接能力。
- 继续由插件自己的语义组装层按 `h2` 模块切分，不改变现有 layout.json 协议。

### 验收标准

- 连续换行不会被意外丢失或拼接成错误结构。
- 表格和嵌套列表能生成稳定 HTML。
- 原始 `<script>` 不会进入预览 DOM。
- 现有模板和 16 项核心测试保持通过。

## 4. P1：受控 customCss

### 当前问题

TemplateSpec 只有固定视觉 Token，AI 无法表达卡片、时间线、侧栏色块等更具体的视觉规则。外部 CSS 虽能加载，但调优样式在后面且大量使用 `!important`，容易造成“写了 CSS 但看不到效果”。

### 本轮方案

TemplateSpec 增加可选 `customCss`，但它不是任意 HTML/CSS 执行入口：

- 只允许 CSS 文本，不允许 `<style>`、`<script>`、`@import` 和外部资源 URL；
- 限制长度，拒绝明显的行为型 CSS；
- 自动放在当前模板作用域内；
- 插入在 Renderer 基础样式之后、调优 Token 之前，让用户手动调节仍然有效；
- 保存前走 Schema 校验，渲染后继续走 A4 测量。

### 验收标准

1. AI 生成的卡片、侧栏和时间线规则能在预览中生效。
2. customCss 不能注入脚本或跨模板污染。
3. 手动字号、行高、页边距调整仍能覆盖模板默认值。
4. 旧模板没有 customCss 时行为不变。

## 5. 暂不在本轮处理

- 超高模块按 `li` 自动拆分；
- 基于模块高度的智能 autotune 搜索；
- preview-api 和 client 的完整浏览器集成测试。

这三项仍列为后续 P2/P3，避免把本轮 P0/P1 的稳定性改动和更大范围的分页策略混在一起。
