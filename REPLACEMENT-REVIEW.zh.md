# dsh-resume 平替 CodeCV 能力复盘与对接方案

## 1. 复盘结论

本项目平替的是 CodeCV 解决的用户问题，不是复制它的源码、语法、DOM、CSS、模板资产或交互文案。

目标结果是：用户提供真实经历和目标岗位，DeepSeek 负责理解、规划、生成与复核，工作台负责稳定渲染和测量，用户最终得到一份视觉层级清晰、密度平衡、适合 A4 投递的简历。

当前已有模板预设、模板 Schema、模板库缩略图、A4 预览、手动调节和基础一页检测；尚缺 AI 新模板的保存闭环、扩展模块注册、布局侧车文件和基于测量结果的自动迭代。

## 2. 能力对照

| 用户能力 | dsh-resume 的独立实现 |
| --- | --- |
| 编辑简历内容 | 普通 Markdown，内容与视觉模板分离 |
| 调用扩展内容 | `resume.layout.json` 声明模块类型、数据来源和区域 |
| 切换视觉模板 | `templates/*.json` 的 TemplateSpec，由固定 Renderer 执行 |
| 生成新模板 | AI 生成 TemplateSpec，校验后保存到工作区 |
| 模板缩略图 | 根据模板 Schema 生成代码原生缩略图，不复制图片资产 |
| 预览 | 独立 A4 页面树和确定性分页逻辑 |
| 一页通过 | 页数、溢出、使用率、留白、模块断裂综合判断 |
| 手动调优 | 字号、行高、模块间距、页边距、撤销和恢复默认 |
| 导出 | 用户在预览区确认后导出 HTML/PDF |

## 3. 目标架构

```text
resume.md                 内容
resume.layout.json        结构与模块调用
templates/*.json          视觉 Token 与模块变体
extensions/*.json         可选扩展模块配置
        ↓
独立 Markdown/布局解析器
        ↓
模块 Renderer 注册表
        ↓
A4 渲染、分页和视觉测量
        ↓
DeepSeek 根据证据提出下一轮调整
```

### 3.1 内容层

`resume.md` 保持普通 Markdown，不要求用户学习 `@dsh` 指令，也不把模板布局写进正文。相同的内容可以复用到多个模板和多个公司版本。

### 3.2 结构层

布局侧车文件负责表达 Markdown 不适合表达的结构：栏位、模块顺序、显示条件、模块变体和扩展数据。例如：

```json
{
  "schemaVersion": 1,
  "regions": {
    "main": ["education", "projects", "experience"],
    "side": ["profile", "skills", "links"]
  },
  "blocks": [
    {
      "id": "projects",
      "type": "project-list",
      "source": "项目经历",
      "options": { "showMetrics": true, "showTechTags": true }
    },
    {
      "id": "skills",
      "type": "skill-tags",
      "source": "专业技能",
      "options": { "style": "pill" }
    }
  ]
}
```

未知模块必须降级为普通内容，不能导致整份简历无法渲染。

### 3.3 模板层

模板只描述受控的视觉参数和模块变体，不允许 AI 直接写入任意脚本。每个模板先经过 Schema 校验，再交给固定 Renderer 渲染。

模板生成流程：

```text
用户描述风格
→ DeepSeek 生成 TemplateSpec
→ jobhunt_template_validate
→ jobhunt_template_save
→ 预览与缩略图
→ 根据测量结果调整
→ 用户确认并保存
```

## 4. “一页通过”的定义

只有“页面数等于 1”不能算通过。通过条件必须同时包含：

1. 页数为 1；
2. 内容没有溢出；
3. 页面使用率位于可读的目标区间；
4. 底部不是明显空白；
5. 章节标题和项目块没有孤立或断裂；
6. 重点模块的视觉密度基本一致。

状态至少分为：

- `overflow`：溢出或被迫分页；
- `sparse`：一页但留白明显；
- `fit`：一页且密度平衡；
- `multi`：多页但没有溢出错误。

## 5. 大模型与确定性引擎的分工

大模型负责：

- 识别内容结构和岗位重点；
- 选择或生成模板；
- 规划模块顺序、栏位和变体；
- 解释留白、拥挤、断裂等视觉问题；
- 根据测量结果提出下一轮调整；
- 在不编造事实的前提下优化表达。

确定性引擎负责：

- Markdown 和布局解析；
- 模块渲染；
- A4 分页；
- 溢出、留白和模块断裂测量；
- HTML/PDF 预览基础；
- 模板和布局 Schema 校验。

原则是：AI 决策，Renderer 执行，测量结果反哺 AI。

## 6. 当前实现与缺口

### 已完成

- 5 个原创视觉预设：标准、技术、编辑、终端、侧栏聚焦；
- 模板库和缩略图；
- TemplateSpec Schema 与校验；
- 模板切换和 A4 预览；
- 手动调节和一页密度提示；
- 内部渲染命名与参考项目进一步隔离。

### 已完成的第一轮 P0

- 自定义模板 JSON 保存到 `jobhunt/templates/`；
- 自定义模板在模板库中读取和预览；
- DeepSeek 通过结构化工具生成模板，而不是手写 CSS；
- 模板生成后的校验、渲染和测量闭环；
- 布局侧车文件 Schema 和扩展模块注册表的第一版；
- 浏览器测量结果回传给 Agent；
- 最多 3 轮受控自动调优；
- 双栏 Renderer、模板工坊、复制、版本和撤销。

### 下一轮 P1

- 双栏下的独立模块分页与断裂测量；
- `project-list`、`skill-tags`、`timeline`、`metric-row` 等模块 Renderer；
- AI 自动调节并生成前后版本对比；
- 自定义模板复制、重命名、版本和回滚；
- JD 定向的多投递版本。

### P2

- 模板导入导出；
- 中英文简历；
- 多页简历；
- 用户分享模板。

## 7. 独立实现边界

- 不复制参考项目的源码、函数名、DOM 类名、CSS 选择器、模板文件、图片资产或原有文案；
- 不把参考项目的自定义语法作为本项目的公开格式；
- 只借鉴公开的产品问题、可观察的用户流程和通用排版目标；
- 本项目的 Schema、解析器、Renderer、测量指标、UI 和模板视觉均独立设计；
- 对参考项目的具体实现只做风险排查，不作为代码来源。

## 8. 验收标准

用户可以说：

> 做一个适合前端实习投递的黑白高密度模板，保留项目成果指标，最终刚好一页。

插件应能完成：

1. 生成合法模板 JSON；
2. 保存并出现在模板库；
3. 生成缩略图；
4. 将模板应用到当前简历；
5. 报告页数、使用率、留白和断裂情况；
6. 根据测量结果给出下一轮调整；
7. 用户手动微调并撤销；
8. 用户确认后导出。
