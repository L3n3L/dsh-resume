# dsh-resume 视觉调优对接复盘

> 状态：第二期主线文档
> 日期：2026-08-22
> 产品方向：视觉复核与 A4 简历排版调优

## 1. 结论先行

当前插件不应该继续扩展成 JD 分析器或简历内容评分器。用户的核心目标是：

> **把已有简历生成一份接近最终投递效果的视觉稿，并持续调整到符合自己的复核预期。**

因此产品应该定位为：

> **面向投递场景的 A4 简历视觉复核与排版调优工作台。**

内容优化是 Agent 的辅助能力，视觉结果才是插件的主结果。

## 2. 用户真正要解决的问题

用户通常不是不知道自己写了什么，而是不确定：

- 这份简历打印出来是否真的只有一页；
- 页面为什么看起来很挤或很空；
- 标题、项目、时间和结果的层级是否清楚；
- 哪个模块把第二页顶出来了；
- 字号、行距、模块间距应该怎么调；
- 调整后是否比上一版更接近自己的预期；
- 屏幕预览和最终 PDF 是否一致。

插件必须直接回答这些问题，而不是只给一个“检查通过”。

## 3. 第一阶段复盘

### 已完成

- Markdown 简历读取与渲染。
- A4 纸张预览与打印。
- 模块拆分和页面状态提示。
- 工作台式预览 UI。
- 基础质量检查 API。

### 需要降级为辅助能力

- JD 匹配评分；
- 联系方式完整性评分；
- 泛化的“适合投递”判断；
- 没有对应视觉动作的内容提醒。

这些信息可以保留，但不能占据主界面，也不能代替视觉复核。

## 4. 视觉调优闭环

```text
读取简历
  ↓
生成 A4 视觉稿
  ↓
测量实际页面
  ↓
识别视觉问题
  ↓
给出可执行调整
  ↓
用户选择是否应用
  ↓
重新渲染并对比上一版
```

每次调优必须产生三类结果：

1. 当前页面的客观测量；
2. 问题对应的具体原因；
3. 可撤销的调整建议。

## 5. 视觉指标设计

### 5.1 页面指标

- 实际页数；
- A4 内容区高度；
- 最后一页有效内容高度；
- 页面底部剩余空白；
- 是否存在内容溢出；
- 是否出现只有标题或单个模块的孤立页面。

### 5.2 结构指标

- 一级标题与联系方式是否形成清晰页眉；
- 二级模块之间的间距是否一致；
- 项目标题、时间、角色是否处于同一视觉层级；
- 列表缩进是否一致；
- 是否出现标题与正文分离；
- 是否出现一个模块被不自然地拆到两页。

### 5.3 密度指标

- 正文平均字号；
- 行高；
- 模块间距总量；
- 页面有效内容占比；
- 空白区域占比；
- 单页信息密度是否过高。

这些指标用于解释和调优，不用于伪造一个绝对的“审美分数”。

## 6. 调整策略

调优顺序必须固定，避免 Agent 一上来就把字号缩小：

1. 删除重复或低信息量内容；
2. 合并过短的项目要点；
3. 调整模块顺序；
4. 调整模块间距和列表间距；
5. 调整页边距；
6. 调整行高；
7. 最后才调整字号。

### 调整建议示例

```text
当前结果：2 页
问题：第二页只有“实习经历”模块，高度 188px
建议：
1. 将实习经历模块移到第一页项目经历之后；
2. 模块间距从 20px 调整为 12px；
3. 若仍超出，再减少项目列表中的重复描述。
```

建议必须说明：

- 调整对象；
- 调整前后数值；
- 预计影响；
- 是否可能损害可读性；
- 是否可以撤销。

## 7. UI 对接方案

### 7.1 中央区域：A4 视觉画布

- 固定纸张比例；
- 页面之间有明确间隔；
- 支持适应宽度、75%、100%缩放；
- 显示页码；
- 支持滚动定位到问题页面；
- 画布与打印结果使用同一套 CSS。

### 7.2 左侧区域：视觉导航

- 页面缩略图；
- 页面问题标记；
- 模块目录；
- 当前选中模块；
- 一页 / 两页目标切换。

### 7.3 右侧区域：调优面板

分成三层：

1. **结果**：页数、溢出、空白比例、密度；
2. **建议**：系统发现的问题和建议调整；
3. **控制**：边距、字号、行高、模块间距、主题色。

右侧不能只显示静态说明，所有指标都必须来自当前渲染结果。

### 7.4 用户手动调整

手动调整是核心能力。Agent 的建议必须是可选的，不能自动覆盖用户决定。

第一版提供稳定、可复现的控件：

- 页面边距；
- 正文字号；
- 行高；
- 模块上下间距；
- 列表缩进；
- 紧凑 / 标准 / 宽松密度；
- 模板主色和字体。

每次手动调整都必须支持：

- 立即预览；
- 显示调整前后页数变化；
- 撤销本次调整；
- 恢复默认值；
- 保存为当前模板设置。

第一版不优先做自由拖拽，因为拖拽容易导致打印结果不可复现。后续可以在保留数值控制的前提下增加拖拽辅助。

### 7.5 顶部区域：版本与复核

- 当前简历文件；
- 当前模板；
- 上一版 / 当前版切换；
- 重新渲染；
- 应用调整；
- 撤销调整；
- 打印 / 导出 PDF。

## 8. Agent 的新角色

Agent 不再被描述为“简历内容评分器”，而是：

> **视觉排版操作员和复核助手。**

Agent 的任务：

- 读取用户的排版目标；
- 识别当前页面的视觉问题；
- 解释问题原因；
- 提出最小修改方案；
- 修改 Markdown 或 CSS；
- 重新渲染；
- 汇报调整前后变化。

Agent 不应该：

- 没有用户同意就大幅改写内容；
- 为了塞进一页无条件缩小字号；
- 用一个分数掩盖具体问题；
- 把内容真实度检查说成视觉已经合格。

## 9. 第二期实施拆分

### P0：必须先做

1. 从 iframe 内实际测量页面和模块高度；
2. 输出页面级诊断结果；
3. 增加缩放和页码导航；
4. 增加“适应宽度 / 100%”预览控制；
5. 将右侧状态改为实际测量值。
6. 提供边距、字号、行高和模块间距的手动控制。
7. 支持撤销调整和恢复默认值。

### P1：核心调优

1. 紧凑 / 标准 / 宽松三档密度；
2. 页边距、行高、模块间距控制；
3. 调整前后对比；
4. 应用建议和撤销建议；
5. 一页目标模式。

### P2：增强能力

1. 页面缩略图；
2. 模块拖动排序；
3. 自定义模板保存；
4. 视觉版本快照；
5. 用户上传参考截图后的人工复核辅助。

## 10. 与 CodeCV 的学习边界

可以学习：

- A4 画布和打印尺寸；
- 分页预览；
- 编辑与预览分区；
- 字体、颜色、边距、行高控制；
- 导出前的视觉确认流程。

必须独立实现：

- 分页算法；
- 页面测量逻辑；
- DOM 结构；
- CSS 模板；
- UI 组件、图标、文案和视觉资产。

不得直接复制：

- `common.css`；
- `splitPage`；
- `moduleCombine`；
- `.jufe-*` 页面结构；
- CodeCV 模板、图标、Logo 和截图。

## 11. 验收标准

- 用户能清楚看到每一页 A4 画布；
- 用户能知道具体是哪一页、哪个模块造成溢出；
- 预览不会被裁切，缩放后仍保持页面比例；
- 系统能给出具体的间距、字号或内容调整建议；
- 用户可以手动调整关键排版参数，而不必接受 Agent 建议；
- 手动调整支持即时预览、撤销和恢复默认值；
- 应用调整后可以重新渲染并看到前后差异；
- 用户可以撤销一次调整；
- 屏幕预览和打印 PDF 的页面结构一致；
- 视觉调优功能不依赖复制 CodeCV 的源码或资源。

## 12. 本轮开发决策

从本文件开始，视觉调优是主线。JD 匹配、内容评分、投递状态都降级为后续辅助能力。后续每增加一个功能，必须回答：

> **它是否帮助用户更快判断并调整最终视觉结果？**

如果不能，就不进入本期开发。

## 13. 本轮已落地

- iframe 预览会回传真实页数、页面尺寸、使用高度、留白比例、模块列表和溢出状态；右侧“版式结果”读取这组测量结果。
- 右侧新增“手动调整”：字号、行高、模块间距、页边距均可用滑杆即时调整。
- 支持撤销最近的视觉调整、恢复默认值；导出 HTML 和打印会沿用当前预览参数。
- 调整参数通过预览 URL 传入，并在渲染页内以独立覆盖样式生效，不要求用户复制某个外部项目的模板代码。
- 本轮仍未做自由拖拽、模块排序和持久化视觉快照，这些保留到后续版本，避免把“能调”做成不可控的编辑器。
