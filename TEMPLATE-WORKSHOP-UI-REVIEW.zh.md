# 模板工坊 P0 对接复盘

## 1. 产品判断

模板工坊的目标不是把普通用户变成 CSS 开发者，而是让用户可以用三种难度创建自己想要的简历模板：

1. 描述需求，让 AI 生成 `TemplateSpec + 独立 templateCss`；
2. 用视觉 Token 和实时 A4 预览快速微调；
3. 需要时打开高级 CSS 编辑器，接管细节。

因此 CSS 编辑器是高级入口，不是默认主流程。默认流程仍然是“描述视觉 → AI 生成 → 真实简历试妆 → 保存模板”。

## 2. 本轮解决的问题

此前模板系统已经支持：

- `templates/<id>.json` + `templates/<id>.css` 双文件保存；
- `jobhunt_template_generate` 生成 JSON 和 CSS；
- `jobhunt_template_save` 校验并记录历史版本；
- A4 预览和视觉 Token 实时调整。

但工坊 UI 只暴露了 JSON，用户无法从面板编辑独立 CSS，也无法在保存前看到 CSS 错误。这造成“后端支持模板创作，普通用户却无法操作”的断层。

## 3. P0 交互方案

### 3.1 默认层

保留现有视觉 Token 面板：颜色、圆角、分隔线、字号和间距。用户无需理解 CSS，就能快速改变模板风格。

### 3.2 高级层

增加可折叠的“编辑模板 CSS”抽屉，默认不占用主界面空间。抽屉包含：

- CSS 文本编辑区；
- 当前字符数；
- 当前模板作用域提示；
- 校验 CSS；
- 撤销编辑；
- 恢复已保存版本；
- 保存当前 CSS；
- 内置模板使用“另存为模板”，避免覆盖内置资产。

### 3.3 实时预览

编辑内容不直接写入工作区文件，而是通过受控 `postMessage` 临时注入右侧 A4 iframe：

```text
CSS 输入变化
  → 模板工坊状态更新
  → postMessage(templateCss + visualTokens)
  → A4 iframe 临时 style
  → 用户立即看到结果
```

只有点击保存后才会调用模板保存 API，失败不会污染工作区。

## 4. 安全与数据边界

- 保存前调用现有 `validateTemplateSpec` 和 `validateCssText`；
- 禁止外部 URL、`@import`、脚本、行为型 CSS；
- 允许的资源仍然只有内联图片 data URI、片段和本地 asset API；
- CSS 预览只存在当前 iframe 会话，不自动落盘；
- 内置模板不能被直接覆盖，只能另存为自定义模板；
- 自定义模板沿用现有历史版本机制，保存前自动记录上一版本。

## 5. 模板生命周期

```text
内置模板
  → 另存为
自定义草稿
  → CSS / Token / AI 调整
已保存模板
  → 用户主动选择
当前模板库模板
```

当前 P0 不把 AI 新生成的模板自动加入内置库，也不把简历正文写进模板文件。模板只保存结构和视觉，简历内容仍然独立存在。

## 6. 对 AI 的协作契约

当用户说“做一个墨绿商务侧栏模板”时，AI 应该执行：

1. 生成设计意图和合法 `TemplateSpec`；
2. 生成独立 CSS；
3. 调用保存工具；
4. 渲染真实简历；
5. 读取 A4 页数、溢出和留白；
6. 必要时迭代 CSS、Token 或布局；
7. 在插件中加载最终模板。

AI 不应只返回一段 CSS 代码让用户自己复制，也不应把内置模板直接覆盖。

## 7. 验收标准

1. 模板工坊能读取当前模板的独立 CSS；
2. 编辑 CSS 后右侧 A4 iframe 即时变化；
3. 合法 CSS 校验显示通过；
4. 外部 URL 等危险 CSS 显示可读错误，且不保存；
5. 内置模板点击保存会进入“另存为模板”；
6. 自定义模板可以保存当前 CSS，并进入历史版本；
7. 恢复已保存版本可撤销未保存编辑；
8. 37 个原有测试继续通过；
9. npm 打包包含本复盘文档。

## 8. 后续边界

P0 不引入完整可视化拖拽排版器，不允许用户直接编辑任意 HTML，也不做模板市场。后续可以在现有基础上增加：

- CSS 语法高亮编辑器；
- 当前预览另存为模板；
- 版本差异对比；
- AI 根据截图或排版指标提出 CSS 修改建议；
- 模板导入导出和社区分享。
